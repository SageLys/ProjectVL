# 可玩原型 · 重启开发总计划（回退后 v1）

日期：2026-07-12
状态：待用户确认后按序派发，本文档只做计划、不执行
起点：仓库已**主动回退到 P4 开始前**（= P3 完成态：六域配置 + effects 解释器 + 108 测试绿 + 5 张占位卡）。P4/P4.1/P5 的代码与数值结论全部废弃。
上游依据（派发任何 prompt 前 agent 必读）：`docs/P2_技能体系框架与首批卡牌设计表.md` v2.0、`docs/skills-schema.json` v0.4.0、`docs/P2_交互重设计_线框与输入层改造清单.md` v2.0（含 §9 实装增量）、`docs/P6_体验重标定_反思与重构计划.md`（教训）、`docs/P6_R1_手动调参环_执行计划.md`（S3 阶段直接复用）。

> **2026-07-16 修订（万能卡独立机制）**：`resonance` 卡已删除，不入卡池、不建卡实例。万能卡已改为独立资源与拖拽合成机制；本阶段仅保留 DEV 发放入口，任何 Boss/Bounty/普通掉落来源均未接入。S4b 仍负责真卡经济建模与 E3/E6 回填。
> **装备语义已拍板（2026-07-14）＝选项B：装备可撤销/可消耗**。装备着的卡可随时拖入战场消耗释放（触发消耗态即时效果并腾出装备位），`equipIrreversible=false`、`unequipPolicy="consume"`，不设无效果的纯销毁入口。schema v0.4.0 与代码已是此语义；P2 设计表 R11/§4、本文档、S4a 均已对齐。旧「永久不可撤销/卸下=纯销毁」作废。R11 新义见 `docs/P2_技能体系框架与首批卡牌设计表.md` §5。

---

## 0. 两条铁律（写进每个阶段的验收门）

**铁律一：界面必须真的换掉。** 上一版最致命的执行事故：直到最终可试玩版本，界面仍是最初的横屏桌面布局，2.1 设计的竖屏交互从未落地。本次约定：

- 旧界面（横屏布局、旧 dock、旧临时结构残留）**直接删除，不做隐藏、不留开关、不留"以防万一"的兜底**。删不掉说明耦合有问题，修耦合而不是留旧码。
- 一切 UI 阶段的验收 = **对照 `docs/wireframes/交互线框_方案A_独立装备格.svg` 逐项截图打勾**，并 grep 证明旧布局代码/样式/DOM id 零残留。
- UI 重构排在最前（S1），不许"先做逻辑最后补 UI"。

**铁律二：真人验收是唯一出口。** 每个阶段完成后，用户亲自玩前 90 秒并按检查单（有事做/有东西看/有紧张感）确认，**用户未点头，该阶段不得标记通过**，agent 不得自行开始下一阶段。

---

## 1. 阶段总览

| 阶段 | 名称 | 执行者 | 内容一句话 | 估时 | 你要做的 |
|---|---|---|---|---|---|
| S0 | 真值锁定与差距盘点 | AI | 提交回退态、打 tag、列"设计 v2.0 vs 代码现状"差距清单 | 0.25 天 | 看报告，点头 |
| T1 ✅ | 手机触控校准（2026-07-13 完成） | AI 备工具 + **人** | 实测并回填 8px / 150ms / 30px、长按填充环、双击 | 已完成 | 已验收 |
| S1a | 竖屏三段式 + pointerRouter | AI | 删旧界面，按线框重建竖屏布局与统一输入层 | 2–3 天 | 手机+桌面点验，对照线框 |
| S1b ✅ | 方案A 装备格 + 可消耗装备（选项B） | AI | 装备 3 格、装备可撤销、卸下=消耗释放、economy 增量 | 已完成 | 已验收 |
| S2 | 6★ 星级体系落地 | AI | schema v0.4.0 进解释器（3/5/6 锚点+4★放大+消耗态内插），占位卡迁移 | 1–1.5 天 | 控制台冒烟即可 |
| S3 | 手动调参环 | AI 备工具 + **人** | 按 `P6_R1_手动调参环_执行计划.md` 执行（其 Prompt 1–5） | 3–4 天 | 亲手调波 1–3（主任务） |
| S4a | 6★ 经济拍板（provisional） | **人** + AI | 答 §5 五问（设计意图）→ 设 provisional 经济、不跑仿真、解锁 S5 | 0.25 天 | 答 5 问（约 10 分钟） |
| S5 | 11 卡全量实装（**先于 S4b**） | AI | 批次1（7 张）→ 批次2（4 张）+ bounty；用 S4a provisional 经济 | 3–4 天 | 每批玩一局验卡 |
| S4b | 6★ 经济建模（真卡） | AI | S5 后：merge_sim 按 6 档重写、真人遥测做锚、回填 E3/E6 | 1–1.5 天 | 试玩确认拾取节奏 |
| S6 | 秒级仿真器 + 整局标定 | AI | 无头仿真（E1–E7 地板 + 宏观约束），Boss/后期曲线标定 | 2–3 天 | 玩整局，最终验收 |
| S7 | 收尾与 P7 移交 | AI | 遗留清单、Unity 复刻规格启动包 | 0.5 天 | 确认移交清单 |

合计约 15–20 个工作日（含你的调参时间）。顺序即依赖：**不许跳、不许并行跨阶段**（阶段内的子包可并行）。

为什么 UI 在最前、调参在中间：T1 本来就是 pointerRouter 的硬前置（交互清单 §6）；调参环的 E3/E6（操作节奏）只有在真实竖屏触控交互上调出来才算数——在旧横屏界面上调好的"手感"换皮即失效。这同时封死了"UI 永远排最后所以永远没做"的旧病。

---

## 2. 各阶段说明与验收标准

### S0 真值锁定与差距盘点（AI）

做什么：把回退后的工作区整理提交、打 tag `restart-s0`；逐项比对设计 v2.0 三件（设计表/schema/交互清单§9）与 `src/` 现状，产出差距清单（哪些 P3 已做、哪些待做、哪些设计项与现状冲突）；同时把 `docs/P6_R1_手动调参环_执行计划.md` §0 的"配置不一致"问题标注为"主动回退所致，非事故"。
验收：`docs/S0_差距盘点报告.md` 存在且逐项可勾；tag 已打；`npm test` 全绿；git 干净。

### T1 手机触控校准（AI 备工具，人执行）

做什么（AI）：让 dev 构建可被手机访问（`vite --host` + 二维码/局域网地址说明）；做一个校准测试页/浮层：①点击 vs 拖拽阈值试验（记录你自然点击的位移/时长分布，验证 8px/250ms）②落点预览环偏移试验（Δy 从 40–90px 可调，找不被手指挡住的值）③装备确认形式试验（气泡确认 vs 长按填充环，两种都做出来让你选）。
做什么（人，约 30 分钟）：手机上把三个试验各做 20 次左右，选出：`tapMaxPx`、`tapMaxMs`、`reticleOffsetY` 三个值 + 确认形式二选一 + 双击 vs 长按二选一。
验收：三个值回填 `src/config`（`input` 域）；两个二选一决策写进交互清单 §7 待决问题处（划掉待决）；校准数据留档 `docs/T1_触控校准记录.md`。

### S1a 竖屏三段式布局 + pointerRouter（AI）

做什么：按交互清单 §1、§5（注意：§5 写于 P3 之前，部分项 P3 已完成，先 diff 再做剩余项——主要是 #4 pointerRouter、#9/#10 竖屏布局、#8 预览层）+ 线框 SVG 重建：竖屏三段式（HUD 顶 / 主画面释放区 / 底部 dock 拇指热区）；`pointerRouter.ts` 合并 `pointerDrag.ts` + `dropClick.ts`（T1 回填的阈值；点击=拾取/接 bounty，拖卡入主画面=落点释放，预览环偏移 Δy，拖回 dock=取消）；canvas 逻辑分辨率改竖屏（按线框定，如 540×960；战斗数值随比例粗调到可玩即可，精调归 S3）；**删除全部旧横屏布局与旧输入文件**。
验收：①对照方案A 线框逐区块截图打勾（HUD/主画面/dock 结构、比例、热区）；②手机实测：点击拾取、拖拽释放、取消，全部符合 §1 动词表；③`pointerDrag.ts`、`dropClick.ts` 已删除，grep 旧布局 class/id 零残留；④桌面鼠标同样可玩（同一套 pointer 事件）；⑤`npm test` 绿（输入层新单测补齐）；⑥**你在手机上点验通过**。

### S1b 方案A 装备格 + 可消耗装备（选项B，已完成）（AI）

> **2026-07-14 修订**：本阶段原写「不可逆确认/卸下=销毁」已作废，实际实装并定案为**选项B：装备可撤销/可消耗**（`equipIrreversible=false`、`unequipPolicy=consume`）。以下原始描述保留作历史，语义以本注与 R11 为准。

做什么：按交互清单 §2 + §9：dock 两行=装备 3 格（上）+ 手牌 7 格（下）；装备动词=仅拖入（悬停警示金框+"永久"标签+装备态预览 → 抬指后二段确认，形式取 T1 结论）；卸下=长按已装备格 → 红色销毁确认 → 清空无返还；空装备格 "3★+" 水印；喂养合成（同型同星拖入=升星，不触发确认）。配置增量：`economy.json` 按 §9 修订（`maxStar 6`、`equipThreshold 3`、`handSlots 7`、`equipSlots 3`、删方案B 字段、增 `equipIrreversible`/`unequipPolicy:"destroy"`），`equip-slots` variant 并入 base 后删除；`equipmentSystem` 删 `toggleLock`/locked 语义、增 `unequipDestroy`。
验收：①<3★ 卡拖入装备格被拒绝且有反馈；②装备全流程截图对照线框（含确认气泡）；③确认前取消无副作用，确认后无任何撤销路径；④销毁后格位空、卡消失、无返还；⑤方案B 代码零残留（grep `locked`/`maxLocked`/`toggleLock`）；⑥单测覆盖门槛/确认/销毁/喂养四分支；⑦**你亲手走一遍误触场景**（想装没装成、想取消、手滑碰到销毁）确认防误触成立。

### S2 6★ 星级体系落地（AI）

做什么：schema v0.4.0 进 `src/config/skills` 与解释器：星级键 2/3 → 锚点 3/5/6；4★ = amplifyAxis 纯数值放大（无新机制）；5★ = dual 第二机制；6★ = transform 换形；消耗态按 1/3/6 锚点内插。5 张占位卡迁移到新结构（内容仍占位，不做 12 卡）。合成链路支持 1→6★（二合、普通掉落一律 1★、喂养、同类型唯一均保留为**配置开关**——它们已降级为占位假设，S4 建模后可能翻）。
验收：①解释器单测覆盖 3/5/6 锚点 + 4★ 放大 + 消耗态内插各至少 2 例；②占位卡在 1–6★ 每档可装备（≥3★）/可消耗且效果冒烟通过；③占位假设四项均可由配置翻转且各有单测；④`npm test` 全绿、`tsc --noEmit` 无错。

### S3 手动调参环（AI 备工具，人调参）

**直接执行 `docs/P6_R1_手动调参环_执行计划.md`**，两点适配：①其 Prompt 0（基线核对）已被 S0 吸收，跳过，从 Prompt 1 开始；②调参面板/遥测 HUD 做成竖屏布局上的开发者浮层（prompt 里"先读现状"会让 agent 拿到重构后的真实文件结构）。你的任务见该文档 §5 与文末"快速参考卡"（每回合：玩波 1–3 → 三问打分 → 动 1–2 个滑杆 → 存方案；连续 3 局不同 seed 整体 ≥4 分 → 导出手感基线）。
验收：手感基线三件套产出；KPI v2 定版（E1–E7 门槛回填）；体验目标一页纸 v2。

### S4a 6★ 经济拍板 provisional ✅（2026-07-14 完成）

用户收敛：§5 五问里**只 Q5 有用**，其余都从「每分钟掉落期望」派生、留 S4b。定案：**dropChance=0.27**，锚=**普通掉落 ≈ 55/min**（=调参面板派生读数，实为全 8 波全局均值）。同时把满意 preset「budget模式初版」**焙入 `src/config/base/*.json`**（此前 base 仍是 P3 旧值、无自动加载，fresh build 跑旧无聊配置）；6 个 config 耦合测试随之更新。详见 `docs/S4a_经济拍板_provisional.md`。

### S4b 6★ 经济建模（AI）——**S5 之后**做

前置：S5 已实装 11 卡 + bounty，且有真卡的真人游玩遥测。做什么：`docs/sim/merge_sim.py` 按 6 档重写（旧脚本按 3★ 写，作废）；以 **S5 后真卡实测拾取节奏**（drops/min、星级分布）为锚，且对齐 S4a 锚：普通掉落全局均值 ≈ 55/min @ dropChance 0.27（注意 S4a 澄清——面板 55/min 是全 8 波均值非波1值，真人波1约 22/min，须按逐波掉落速率建模不套平值）；反推 dropChance / 星级掉落分布 / 可达性解法（定向保底、Boss 高星直掉等）；**同时回填 KPI v2 的 E3/E6 操作层**（衔接 `P6_R1` Prompt 4b）。输出进 config，不许破坏 E3/E6 手感地板（第二轮调参校验脚本回归）。
验收：①报告含"拍板目标 vs 仿真达成"对照；②E3/E6 回填并过回归；③你试玩确认节奏没变味。

### S5 12 卡全量实装（AI，两批交付）

前置：S4a 已定 provisional 经济（**不等 S4b**）；装备语义分歧已拍板（见顶部 banner）。做什么：按设计表 v2.0 §8 与 schema v0.4.0 实装。批次1 = 贯穿/连锁闪电/冰霜/诱饵/灼痕/丰收/庇护（7 张，含教学卡贯穿/丰收）→ 你验收 → 批次2 = 分裂爆破/冲击/圣域/荆棘（4 张）。同步实装精英 Bounty 机制（设计表 §6：接单=集火+狂暴，掉落肥而急，opt-in）。每张卡 = 纯 JSON 数据 + 解释器执行，禁止每卡硬编码 if（R 系硬规则逐条自检）。
验收（每批）：①每卡 1–6★ 六档 × 装备/消耗两态冒烟测试表（截图或无头测试输出）；②R1–R11 硬规则逐条自检表；③bounty 全流程可玩（接单/不接单两分支）；④`npm test` 全绿；⑤**你各玩一局**，确认卡牌读得懂、用得出、有爽点。

### S6 秒级仿真器 + 整局标定（AI）

做什么：无头仿真器（复用 core 纯规则层 + 注入 bot）：秒级时间线导出（同屏数序列/事件间隔/威胁距离/击杀位置），bot 画像用 S3 真人遥测校准（反应延迟/拾取时序取实测分布，不拍脑袋）；KPI 汇总 = E1–E7 地板（每波+前 90 秒切片，用 S3 定版门槛与校验脚本）+ 宏观约束（通关率 15–25%、10–14 分钟、过期 ≤20%，作约束不作目标）；Boss 与后期曲线标定（波 4+ 难度递进、Boss 时长上限）。冲突处理原则：**改结构迁就手感**。
验收：①全波 E1–E7 PASS 且前 90 秒切片单独 PASS；②宏观约束满足或冲突项列清单待你裁决；③固定 seed 仿真可复现；④**你玩完整一局**，按检查单全程 ≥3 分、整体 ≥4 分——这是可玩原型的最终验收门。

### S7 收尾与 P7 移交（AI）

做什么：遗留项归档（PC 快捷键、断点续局、局外成长最简占位、无尽模式数值验证、卸下冷却等——只列清单不实装）；产出 Unity 复刻规格启动包（core 层接口清单、配置 schema 汇总、KPI 定版表、交互规格）。
验收：移交清单你确认无遗漏；`docs/P7_Unity复刻规格_启动包.md` 存在。

---

## 3. 你的极简流程卡（每阶段发完 prompt 后做什么）

- **S0**：看差距报告 → 点头。
- **T1**：拿手机玩 30 分钟测试页，选出 3 个数值 + 2 个二选一。
- **S1a**：手机上点一点、拖一拖，对照线框看像不像 → 点头。
- **S1b**：故意手滑试误触，确认装错不了也毁错不了 → 点头。
- **S2**：不用玩，看测试报告 → 点头。
- **S3**：**你的主任务**，亲手调波 1–3（见调参环文档快速参考卡）。
- **S4a**（现在）：答 §5 的 5 个问题（约 10 分钟）→ 解锁 S5。
- **S5**：每批玩一局，用卡、合卡、装卡 → 点头两次。
- **S4b**（S5 后）：建模完试玩一局确认拾取节奏没坏 → 补回 E3/E6。
- **S6**：玩完整一局打分 → 这一关过了，原型就算成了。
- **S7**：看移交清单 → 点头。

---

## 4. 与既有文档的关系

- `P6_R1_手动调参环_执行计划.md`：整体保留，作为 S3 的执行文档；其 §0/Prompt 0 由 S0 取代（"配置不一致"已确认为主动回退，非事故）。
- `P6_体验重标定_反思与重构计划.md`：教训与 E1–E7 定义继续有效；其 R2/R3/R4 并入本计划 S3/S6；"明确不重做"清单中"方案B"一项已被人工定案推翻（方案A 为基座）。
- 交互清单 §5 逐文件表：写于 P3 之前，S1a/S1b 的 prompt 要求 agent 先 diff 现状再执行剩余项，不照抄。

## 5. S4a 的 5 个拍板问题 + 我的 provisional 建议（现在答）

1. 一局期望产出几件 6★？（0–1 件=终局大招定位 / 2–3 件=常规构筑目标）
2. 首件 6★ 期望出现在第几波 / 第几分钟？
3. 4★、5★ 的期望件数各是多少？（决定中期成长曲线的密度）
4. 纯二合下 6★ = 32 张同型 1★，旧拾取锚 38–45 次/局下单卡型基本不可达。你接受哪种解法方向：加大拾取量 / 定向掉落保底 / 高星直掉（Boss/bounty）/ 部分档位三合改二合？（可多选，仿真会比较）
5. 拾取锚本身是否随 S3 手感基线更新？（建议：是，以实测 drops/min 为准）


### 实际拍板（2026-07-14）——收敛为单锚，详见 `docs/S4a_经济拍板_provisional.md`

用户判断：Q1–Q4 都从「每分钟掉落期望」派生，本轮不单独拍，留 S4b 反推。**Q5 = dropChance 0.27 + 锚 55/min（普通掉落，面板全局均值口径）**。以下为原始建议值，仅作 S4b 起点参考：

### 原 5 个 provisional 建议（存档参考）

1. **一局几件 6★ → 建议 0–1 件（终局大招定位）。** 6★=transform 换形，是构筑顶点；稀有化保住"追求感"，也和"溢出高星=零机会成本终局大招"的既有思路一致。
2. **首件 6★ 时机 → 建议第 7–8 波 / 第 9–12 分钟（末段/Boss 前后）。** 让 6★ 成为收尾爆点，而非中期就摊平。
3. **4★/5★ 件数 → 建议 4★ 1–2 件、5★ 0–1 件。** 中后期攒出一个 4★ core（amplify 放大轴）、偶尔摸到 5★ dual 第二机制。
4. **可达性解法 → 建议「定向掉落保底 + Boss/bounty 高星直掉」，保持二合、不改三合。** 纯二合下 6★=32 张同型不可达；用"拾取/掉落向已持有类型倾斜的保底"缩短同型收集，加"Boss/bounty 掉 2★+"给高星注入点。三合会让门槛更高，不选。**这些只是方向，具体权重留 S4b 仿真定。**
5. **拾取锚 → 建议：暂用当前 preset 值（dropChance 0.27）作 provisional，S4b 用 S5 后真卡实测 drops/min 复核。** 现有游玩数据不足（见 KPI v2 §0），此刻不改。

> 这 5 条是**设计意图靶**，不是数值定版；S4a 只据此设 provisional 开关解锁 S5，真正的经济标定在 S4b（真卡+真人遥测）完成。你可逐条改，改完我更新 S4a 记录。

## 6. 风险与预案

1. **S1a 是最大工程风险**（唯一 L 级 + 删旧重建）：prompt 已拆出"先 diff、后分步交付"的要求；若 agent 交付物与线框不符，退回重做，不接受"先凑合"。
2. **竖屏化使 S3 前游戏暂时难看难玩**：S1/S2 阶段的真人验收只验交互正确性，不验"好玩"（好玩归 S3）。
3. **6★ 可达性无解**（所有解法都破坏手感地板）：S4 输出冲突报告，升级为结构问题（合成规则/局长）由你裁决。
4. **手机性能**（同屏 20+ × 竖屏高分屏）：T1 起每个阶段的手机点验都看 HUD FPS；跌破 50 立即开性能整改子任务。
5. **工程注意**：本项目文件改动一律经 bash 写入挂载路径（Cowork 文件工具对变长文件有截断 bug，见 P3 教训）；修改前先与 git HEAD 比对文件完整性。

---

## 7. 可直接复制给 AI Coding Agent 的 Prompts

> 按序派发，前一个验收通过再发下一个。每个 prompt 自包含。S3 阶段改发 `P6_R1_手动调参环_执行计划.md` §8 的 Prompt 1–5。

### Prompt S0 —— 真值锁定与差距盘点

```text
仓库：ProjectVL（塔防原型，Vite+TS；core 纯规则层+注入 RNG+GameEvent，必须保持）。
背景：主分支已主动回退到 P4 开始前（=P3 完成态），P4/P4.1/P5 代码与结论废弃。设计侧已按人工定案修订：6★ 上限、3★ 入装、交互基座=方案A独立装备格；装备语义 2026-07-14 定为选项B（可撤销/可消耗、`unequipPolicy=consume`）。
任务：
1. 整理并提交当前工作区（合理拆分 commit），打 tag：restart-s0。
2. 逐项比对以下设计文档与 src/ 现状，产出 docs/S0_差距盘点报告.md：
   - docs/P2_交互重设计_线框与输入层改造清单.md v2.0（重点 §5 逐文件表——标注哪些项 P3 已完成、哪些待做——与 §9 实装增量）
   - docs/skills-schema.json v0.4.0（星级锚点 3/5/6、amplifyAxis、消耗态 1/3/6 锚点 vs 解释器现状 v0.3.0 结构）
   - docs/P2_技能体系框架与首批卡牌设计表.md v2.0（12 卡 vs 现有 5 张占位卡）
   报告格式：每项 = 设计要求 / 代码现状 / 差距 / 归属阶段（T1/S1a/S1b/S2/S5，阶段定义见 docs/可玩原型_重启开发总计划.md）。
3. 在 docs/P6_R1_手动调参环_执行计划.md §0 顶部加一行注：配置不一致已确认为主动回退所致（回退到 P3 完成态），非事故；其 R1-0/Prompt 0 由本次 S0 取代。
禁止：修改任何游戏数值或逻辑；删除任何文件。
验收：报告存在且逐项含四列；tag 已打；npm test 全绿；git status 干净。完成后停止，等待人工验收。
```

### Prompt T1 —— 手机触控校准工具

```text
仓库：ProjectVL。先读：docs/P2_交互重设计_线框与输入层改造清单.md §1（8px/250ms 阈值、预览环 Δy≈60px 初始值、二段确认形式待定）、§6（T1 是 pointerRouter 硬前置）、§7（待决：气泡确认 vs 长按填充环、双击 vs 长按）、docs/S0_差距盘点报告.md。
任务：
1. 让 dev 构建可被同局域网手机访问：npm script（vite --host）+ 控制台打印局域网地址与二维码（可用小型二维码库，仅 DEV 依赖）。
2. 做校准测试页（路由 /calibrate 或 URL 参数进入，仅 DEV）：
   a. 点击阈值试验：全屏点靶，记录每次 pointer 事件的位移(px)/时长(ms)，实时显示分布直方图与建议阈值；
   b. 预览环偏移试验：按住拖动时显示落点预览环，Δy 滑杆 40–90px 可调，用户找到"不被手指挡住且不觉得飘"的值；
   c. 确认形式试验：同一个"装备"模拟格，做两种确认交互原型（抬指后气泡二段确认 / 长按填充环），可来回切换体验；另做双击 vs 长按对比试验。
3. 每个试验有"导出结果"按钮，汇总写 docs/T1_触控校准记录.md（含原始分布数据）。
4. 试验选定后的回填入口：src/config 新增 input 域（tapMaxPx/tapMaxMs/reticleOffsetY + confirmStyle/holdOrDbl 两个枚举），初值取清单默认（8/250/60），待人测后改。
禁止：动游戏本体逻辑与布局（校准页独立）；生产构建包含校准页。
验收：手机扫码可进校准页并完成三试验；导出记录可用；input 域生效可被后续代码读取；npm test 绿。完成后停止——人将用手机实测约 30 分钟并回填数值，回填后才进 S1a。
```

### Prompt S1a —— 竖屏三段式布局 + pointerRouter（删旧界面）

```text
仓库：ProjectVL。先读：docs/可玩原型_重启开发总计划.md §0 铁律一与 §2-S1a、docs/P2_交互重设计_线框与输入层改造清单.md §1/§5/§6、docs/wireframes/交互线框_方案A_独立装备格.svg、docs/S0_差距盘点报告.md（哪些 §5 项已在 P3 完成，不要重做）、docs/T1_触控校准记录.md（阈值与 Δy 实测值已回填 src/config input 域）、src/input/*、src/ui/*、src/render/*、index.html、src/styles/app.css。
背景教训：上一版直到最后界面都没换成竖屏设计。本次要求：旧横屏布局直接删除，不许隐藏、不许留开关兜底。
任务：
1. 竖屏三段式重建 index.html + app.css：HUD 条（顶）/ 主画面释放区（中，canvas）/ 底部 dock（拇指热区）。布局、比例、区块关系以方案A线框 SVG 为唯一依据（dock 内部装备格本阶段先占位空格，交互在 S1b）。canvas 逻辑分辨率改竖屏（按线框比例定，如 540×960）；combat.json 的 canvas/turret/range 等按新分辨率等比粗调到"可玩不崩"即可，精调属 S3 调参环，禁止在本阶段追求手感。
2. 新建 src/input/pointerRouter.ts，合并并删除 pointerDrag.ts 与 dropClick.ts：
   - pointerdown 后位移 < input.tapMaxPx 且时长 < input.tapMaxMs 判点击，否则拖拽；
   - 点击主画面：命中拾取半径=拾取最近掉落；命中 bounty=接单（机制未启用时预留分支）；
   - 拖卡→主画面抬指=消耗释放，落点=抬指点换算画布坐标；拖拽中显示落点预览环（DOM overlay），圆心偏移手指上方 input.reticleOffsetY，半径读卡牌 AoE 参数，无落点语义卡显示全屏微光；
   - 拖卡→卡格=移动/交换（沿用现逻辑）；拖回 dock/出屏/pointercancel=取消回原格。
3. 删除旧界面：旧横屏布局的 DOM 结构、CSS 类、媒体查询全部移除；调参面板临时收进一个可折叠开发浮层（功能不变，重构属 S3）。
4. 桌面端同一套 pointer 事件天然支持鼠标，验证可玩。
约束：core 纯规则层零改动（输入层只调用既有接口）；每一步保持 npm test 绿；分两个 commit 交付（布局重建 / pointerRouter），便于回退。
验收（PR 描述逐条贴证据）：
a) 手机与桌面截图各一套，对照线框 SVG 逐区块打勾表；
b) grep 证据：pointerDrag.ts、dropClick.ts 不存在；旧布局关键 class/id（从 S0 报告取清单）零残留；
c) 手机实测录屏或逐项自述：点击拾取/拖拽释放/预览环偏移/取消，符合清单 §1 动词表；
d) npm test 全绿 + 输入层新单测（阈值判定、坐标换算、取消路径 ≥6 例）；tsc --noEmit 无错；
e) npm run build 成功，产物无调试/校准代码。
完成后停止，等待人工在手机上点验。
```

### Prompt S1b —— 方案A 装备格 + economy 增量（已执行；语义以选项B为准）

> **2026-07-14 注**：此 prompt 已执行，其中「不可逆/二段确认/卸下=销毁」经调参后改定为**选项B：装备可撤销/可消耗**（`unequipPolicy=consume`）。留档不改，实际语义见 R11。

```text
仓库：ProjectVL。前置：S1a 已验收（竖屏布局+pointerRouter 就位）。先读：docs/P2_交互重设计_线框与输入层改造清单.md §2（方案A 定案细节）与 §9（economy/equipmentSystem 增量清单）、docs/wireframes/交互线框_方案A_独立装备格.svg、docs/T1_触控校准记录.md（confirmStyle 结论）、src/core/systems/equipmentSystem.ts、src/config/base/economy.json、src/ui/slotFactory.ts。
任务：
1. economy.json 按清单 §9 修订：maxStar 3→6、equipThreshold 2→3、handSlots 10→7、equipSlots 0→3、删 maxLocked/equipMode，增 equipIrreversible:true、unequipPolicy:"destroy"；variants/equip-slots.json 内容并入 base 后删除该 variant；方案B 不留 variant。
2. equipmentSystem：删 toggleLock 与 locked 全部语义；恢复独立装备格数据结构；新增 unequipDestroy(slotIndex)（清空格位、卡销毁、无任何返还、产出事件）；装备门槛 ≥ equipThreshold；同类型唯一 + 喂养合成保留（同型同星拖到已装备卡=升星，不触发不可逆确认）。
3. UI/交互（按清单 §2）：dock 两行=装备3格（上，紫框大格）+手牌7格（下）；装备动词仅拖入；拖卡悬停装备格=警示金框+"永久"标签+装备态预览；抬指后二段确认（形式取 T1 结论），确认前明示不可撤销，取消无副作用；卸下=长按已装备格→红色销毁确认（显示将失去的星级/机制）→确认后清空；空装备格常显 "3★+" 淡金水印。
4. 遥测：state.uses 拆 consumes/equipOps（清单 §5-#15）；装备操作耗时与误触埋点（H6 观察项后续用）。
约束：core 层改动仅限 equipmentSystem 及其类型/事件；每步 npm test 绿；方案B 相关旧测试用例删除并补新用例。
验收：
a) <3★ 拖入装备格被拒且有明确反馈；
b) 装备全流程截图对照线框（悬停态/确认态/已装备态/销毁确认态）；
c) 确认后不存在任何撤销路径（代码层证明：无 unequip 返还分支）；喂养升星不弹确认；
d) grep 证据：toggleLock/locked/maxLocked/equipMode 零残留；
e) 单测覆盖：门槛拒绝/二段确认取消/销毁/喂养/同类型唯一 ≥8 例；npm test 全绿。
完成后停止，等待人工点验误触场景。
```

### Prompt S2 —— 6★ 星级体系落地解释器

```text
仓库：ProjectVL。前置：S1b 已验收。先读：docs/skills-schema.json v0.4.0（stars 锚点 3/5/6、amplifyAxis、anchors、消耗态 1/3/6 内插）、docs/P2_技能体系框架与首批卡牌设计表.md §4（星级质变模板：1★素材→3★core 机制成形→4★amplify 纯数值放大→5★dual 第二机制→6★transform 换形）与 §5（R1–R11）、src/core/effects/*（现为 v0.3.0 结构：星级键 2/3）、src/config/base/skills.json。
任务：
1. 解释器与配置结构升级到 schema v0.4.0：装备态按 3/5/6 三锚点定义机制，4★ 沿 amplifyAxis 对 3★ 数值做纯放大（放大系数进 schema，不新增机制），6★ transform 允许替换整个效果集；消耗态按 1/3/6 锚点定义、2/4/5★ 内插规则实现于解释器。
2. 现有 5 张占位卡迁移到 v0.4.0 结构（内容仍为占位语义，不做 12 张正式卡——那是 S5）。
3. 合成链路扩到 1→6★：二合、普通掉落一律 1★、喂养合成、同类型唯一——这四项已降级为占位假设，全部做成配置开关（economy 或 skills 域），默认维持现状，每个开关有单测证明可翻转。
4. schema 校验：写一个加载时校验器（或构建期脚本），卡牌 JSON 不符合 v0.4.0 结构即报错，禁止静默降级。
约束：禁止每卡硬编码 if（R 系硬规则）；core 接口签名不变；npm test 全绿基础上新增用例。
验收：
a) 解释器单测：3/5/6 锚点、4★ 放大、消耗态内插、6★ transform 各 ≥2 例；
b) 占位卡 1–6★ 每档：≥3★ 可装备、全档可消耗，无头冒烟测试输出六档效果表；
c) 四个占位假设开关各有翻转单测；
d) 校验器对一个故意构造的坏 JSON 报错；npm test 全绿、tsc --noEmit 无错。
完成后停止。下一阶段 S3 改发 docs/P6_R1_手动调参环_执行计划.md §8 的 Prompt 1（其 Prompt 0 跳过），并向 agent 附注：调参面板与遥测 HUD 做成竖屏布局上的开发者浮层。
```

### Prompt S4a —— 6★ 经济拍板 provisional（现在派发，附 §5 五答）

```text
仓库：ProjectVL。前置：S3 收口（KPI v2 定版 docs/P6_新KPI体系_v2_定版.md 存在）。先读：docs/可玩原型_重启开发总计划.md §5（含用户 provisional 5 答）、src/config/base/economy.json、docs/P2_技能体系框架与首批卡牌设计表.md §4/§8。
用户 provisional 拍板（派发时粘贴 §5 五答的最终值）：
1) 一局 6★ 期望件数：___  2) 首件 6★ 波次/分钟：___  3) 4★/5★ 件数：___  4) 可达性解法方向：___  5) 拾取锚：___
任务（不跑仿真——真卡未实装，merge_sim 留 S4b）：
1. 据 5 答把 economy.json 的 provisional 值设好并注释来源：placeholderAssumptions 四开关（twoCopyMerge/normalDropsOnlyOneStar/feedEquipped/distinctEquippedTypes）维持现值或按第 4 问方向微调；dropChance 暂用现值（第 5 问）。
2. 第 4 问选定的可达性解法只登记为"待 S4b 实现的候选清单"，本阶段不写实现代码。
3. 产出 docs/S4a_经济拍板_provisional.md：逐问答案 + 理由 + 待 S4b 项清单。
禁止：跑 merge_sim；实装 6★ 可达性系统；动 KPI v2 已定的感知/威胁层参数。
验收：economy.json provisional 值与拍板一致且有注释；S4a 文档存在；npm test 全绿。完成后停止——进 S5。
```

### Prompt S4b —— 6★ 经济建模（**S5 之后**派发）

```text
仓库：ProjectVL。前置：S5 已实装 11 卡 + bounty 并验收，存在真卡的真人游玩遥测（telemetry/session_*.json，新构建）。先读：docs/S4a_经济拍板_provisional.md、telemetry/session_*.json（真卡）、docs/P6_新KPI体系_v2_定版.md、docs/P6_R1_手动调参环_执行计划.md §6 与 Prompt 4b、docs/sim/merge_sim.py（按 3★ 写的旧脚本，作废重写）、src/config/base/economy.json。
任务：
1. 重写 merge_sim.py 为 6 档模型：输入=真卡实测拾取序列（速率/星级分布取遥测，不拍脑袋）、合成规则开关、手牌 7+装备 3；输出=各星级首达时间/终局件数/停滞溢出统计；蒙特卡洛 ≥4000 局/配置。
2. 扫参数空间（dropChance、星级掉落分布、S4a 登记的可达性解法组合），找满足 §5 拍板目标的配置集；生成 config patch。
3. 回填 KPI v2 的 E3/E6 操作层门槛（照 P6_R1 Prompt 4b 算法，此时真卡+≥3 局不同 seed 到位）；候选配置须过 E3/E6 回归，破坏即淘汰。
4. 产出 docs/S4b_六星经济建模报告.md（目标 vs 达成对照、推荐配置、E3/E6 回填结论）；推荐配置单独 commit。
验收：报告逐项对照；E3/E6 回填并过回归；merge_sim 幂等；npm test 全绿；用户试玩确认拾取节奏。
```

### Prompt S5 —— 12 卡全量实装（两批）

```text
仓库：ProjectVL。前置：S4a 已定 provisional 经济（**不等 S4b**）；顶部 banner 的装备语义分歧已拍板。先读：docs/P2_技能体系框架与首批卡牌设计表.md v2.0 §8（12 卡逐卡设计：锚点 3/5/6 装备态+4★轴+消耗态）、§5（R1–R11 硬规则）、§6（精英 Bounty 机制）、§7（点击动词冲突表）、docs/skills-schema.json v0.4.0、src/core/effects/*、src/config/base/skills.json（5 张占位卡将被正式卡替换）。
任务（两批交付，批次1 验收通过才做批次2）：
批次1（7 张）：贯穿 pierce、连锁闪电 chainLightning、冰霜 frost、诱饵 decoy、灼痕 scorch、丰收 harvest、庇护 aegis。每张=纯 JSON 数据+解释器执行，禁止每卡硬编码 if；占位卡 JSON 删除。
批次1 同步：精英 Bounty 机制启用（skills.json mechanisms.bounty）：波 3 起概率刷精英、主画面单击接单=集火+狂暴、赏金掉落肥而急（2★ 概率上调）、不接单无惩罚；与点击动词冲突表 §7 核对拾取/接单优先级。
批次2（4 张）：分裂爆破 splitBlast、冲击 impact、圣域 sanctum、荆棘 thorns。`resonance` 卡已删除，不属于卡池或技能批次。
每批交付都要：
1. 无头冒烟矩阵：每卡 × 1–6★ × 装备/消耗两态，输出效果生效证据表；
2. R1–R11 逐条自检表（尤其 R11 选项B：装备可撤销/可消耗、`unequipPolicy=consume`、不设纯销毁入口；消耗态 ≤5s 禁永久）；
3. 文案接入 src/data/texts.json（卡名/描述按星级档展示）；
4. npm test 全绿 + 每卡至少 2 个行为单测。
约束：解释器如缺原子/触发器，先补原子并加单测，再写卡数据；不动战斗/波次数值（手感基线锁定，卡牌强度问题记录到 S6 标定清单，不擅自改基线参数）。
验收：上述四件套齐 + 用户每批实玩一局点头。完成批次1 后停止等验收，再做批次2。
```

### Prompt S6 —— 秒级仿真器 + 整局标定

```text
仓库：ProjectVL。前置：S5 已验收（12 卡+bounty 在线）。先读：docs/P6_体验重标定_反思与重构计划.md §2/§3（五根因与方法论教训——本阶段就是为堵住它们而存在）、docs/P6_新KPI体系_v2_定版.md、scripts/checkExperienceKpi.*、telemetry/session_*.json（真人遥测）、src/core/updateGame.ts、docs/实证测试计划.md。
任务：
1. 无头仿真器 scripts/headlessSim.ts：复用 core 纯规则层+注入 bot，固定 seed 可复现；bot 画像（反应延迟/拾取时序/释放决策频率）从真人遥测分布拟合，参数与来源写进报告，禁止拍脑袋常数。
2. 秒级时间线导出：同屏数序列（0.25s 采样）、事件间隔、威胁最近距离、击杀位置、操作机会——口径与 checkExperienceKpi 完全一致。
3. KPI 汇总脚本：每局输出 E1–E7 逐波 + 前 90 秒独立切片（用 KPI v2 门槛判 PASS/FAIL）+ 宏观约束（通关率 15–25%、墙钟 10–14 分钟、掉落过期 ≤20%、Boss 时长）。宏观是约束不是目标：若与 E 地板冲突，输出冲突报告建议改结构（波数/时长），禁止反向调低 E 门槛。
4. 整局标定：波 4 至 Boss 的难度曲线与 Boss 战数值（时长上限、控制抗性），扫描配置使 ≥300 局仿真满足全部 E 地板 + 宏观约束；产出推荐 config patch（单独 commit）。
5. 产出 docs/S6_整局标定报告.md：bot 校准依据、全指标结果表、冲突清单（可为空）、遗留风险。
验收：a) 300 局仿真 E1–E7 全波+前90s 切片 PASS 率 ≥95%；b) 宏观约束满足或冲突清单明确；c) 固定 seed 复现一致；d) npm test 全绿。
完成后停止——最终验收=用户完整玩一局并按检查单打分，未点头不得标记通过。
```

### Prompt S7 —— 收尾与 P7 移交

```text
仓库：ProjectVL。前置：S6 用户验收通过（可玩原型达成）。先读：docs/ 全部 P 系文档、docs/实证测试计划.md、src/ 全量结构。
任务：
1. docs/遗留项清单.md：PC 快捷键适配、断点续局、局外成长（单一全局乘数占位）、无尽模式数值验证、卸下消耗节奏（防误毁高星的可选轻确认/冷却）、T2/T3 实证测试执行状态——逐项标注状态与建议时机，只列不做。
2. docs/P7_Unity复刻规格_启动包.md：core 纯规则层接口清单（函数签名+事件表）、配置 schema 汇总（六域+skills v0.4.0+input 域+KPI v2 门槛表）、交互规格（竖屏三段式+pointerRouter 动词表+确认交互，含 T1 实测值）、数值真值表（手感基线+S4/S6 标定终值）、已知偏差与坑（Cowork 文件工具截断 bug 等工程注意事项）。
3. 全仓库整理：临时文件归档、README 更新为当前真实状态、git tag：prototype-complete。
验收：两份文档存在且自洽（引用的文件/数值与仓库实际一致，抽查 10 项无误）；tag 已打；npm test 全绿。
```
