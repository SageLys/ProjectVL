# ProjectVL 文案工单 · 丰饶

> 由 `scripts/exportCopyWorkOrder.ts` 从当前代码生成（最近一次刷新：本轮）。机制句与游戏内「精确效果」逐字一致。表格里的 JSON 路径即 `src/data/texts.json` 写入位置。

---

## `harvest` — 当前名「眷恋」

- 神：丰饶（`plenty`）　类别：经济　标签：utility
- 身份契约：持续标记高价值目标（赏印）并改善掉落规则——丰饶身份的稳定供给方
- 当前 overview：被标记的追求者，心意都归我。

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.harvest.name` | 眷恋 |
| `cards.harvest.overview` | 被标记的追求者，心意都归我。 |
| `cards.harvest.hand.shortByTier.1` | 标记场上最高价值追求者，提高掉落率 |
| `cards.harvest.hand.shortByTier.3` | 每 3 秒多目标标记，拾取驱动循环 |
| `cards.harvest.hand.shortByTier.6` | 拾取心意后立刻追加赏印，循环不断 |
| `cards.harvest.hand.milestones.3` | **眷恋强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.harvest.hand.milestones.6` | **眷恋终态** / 解锁公共 6★ 终极形态。 （fx: transform） |
| `cards.harvest.equip.shortByTier.3` | 已选分支：眷恋 / 广域眷顾 / 拾取眷顾 |
| `cards.harvest.equip.shortByTier.5` | 叠加接口：兑现 / 印记扩散 / 丰收转化 |
| `cards.harvest.equip.shortByTier.6` | 终极形态：赏印经济最大化 |
| `cards.harvest.equip.milestones.3` | **眷恋强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.harvest.equip.milestones.5` | **眷恋再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.harvest.equip.milestones.6` | **眷恋终态** / 解锁公共 6★ 终极形态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；优先锁定指定目标，索敌权重 1.4，目标生命阈值 100%，持续 3 秒，作用半径 100；100% 概率额外生成 1 个掉落物
- **3★**：半径 140；优先锁定指定目标，索敌权重 1.4，目标生命阈值 100%，持续 3 秒，作用半径 100；100% 概率额外生成 3 个掉落物
- **6★**：半径 160；优先锁定指定目标，索敌权重 1.4，目标生命阈值 100%，持续 3 秒，作用半径 100；100% 概率额外生成 5 个掉落物

### 3★ 分支（三选一）

#### `harvestA` — 当前分支名「眷恋」

精确效果：
- 持续生效 → 优先锁定指定目标，索敌权重 1.4，目标生命阈值 100%，持续 3 秒，作用半径 100；掉落概率 +12%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.harvest.harvestA.name` | 眷恋 |
| `evolution.harvest.harvestA.summary` | 持续标记场上最高价值追求者，提高心意掉落率 |
| `evolution.harvest.harvestA.intent` | 核心机制：被动赏印优先目标+掉落率提升 |

#### `harvestB` — 当前分支名「广域眷顾」

精确效果：
- 每 3 秒 → 优先锁定指定目标，索敌权重 1.6，持续 4 秒，作用半径 125
- 持续生效 → 掉落概率 +16%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.harvest.harvestB.name` | 广域眷顾 |
| `evolution.harvest.harvestB.summary` | 每 3 秒同时标记多个追求者 |
| `evolution.harvest.harvestB.intent` | 核心机制：周期触发多目标赏印 |

#### `harvestC` — 当前分支名「拾取眷顾」

精确效果：
- 拾取掉落时 → 优先锁定指定目标，索敌权重 1，持续 2.5 秒，作用半径 105；掉落物存在时间 +22%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.harvest.harvestC.name` | 拾取眷顾 |
| `evolution.harvest.harvestC.summary` | 每次拾取心意后，立刻标记一个新追求者 |
| `evolution.harvest.harvestC.intent` | 核心机制：拾取触发赏印循环 |

### 5★ 分支（三选一）

#### `harvest1x` — 当前分支名「兑现」　接口角色：payoff

精确效果：
- 击杀追求者时（目标同时处于标记状态） → 35% 概率额外生成 1 个掉落物

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.harvest.harvest1x.name` | 兑现 |
| `evolution.harvest.harvest1x.summary` | 有赏印的追求者被击杀时，额外多掉一份心意 |
| `evolution.harvest.harvest1x.intent` | 兑现接口：赏印击杀触发额外心意产出 |

#### `harvest2x` — 当前分支名「印记扩散」　接口角色：spread

精确效果：
- 拾取掉落时 → 优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.harvest.harvest2x.name` | 印记扩散 |
| `evolution.harvest.harvest2x.summary` | 每次拾取心意后，追加一个额外赏印标记 |
| `evolution.harvest.harvest2x.intent` | 传播接口：拾取触发赏印数量扩增 |

#### `harvest3x` — 当前分支名「丰收转化」　接口角色：convert

精确效果：
- 持续生效 → 掉落物存在时间 +65%；掉落物过期时，将其中 12% 转化为收益

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.harvest.harvest3x.name` | 丰收转化 |
| `evolution.harvest.harvest3x.summary` | 心意在地上存续更久，且过期时转化部分价值 |
| `evolution.harvest.harvest3x.intent` | 转化接口：心意时限延长+过期价值转化 |

### 4★ 公共节点

- 数值放大：{"hpThresholdRatio":"*1.1"}

### 6★ 公共节点

- 持续生效 → 掉落概率 +17%；奖励积分获取 +17%；掉落物存在时间 +23%

---

## `fateLoom` — 当前名「命运织机」

- 神：丰饶（`plenty`）　类别：经济　标签：utility, domain
- 身份契约：合成事件（onMerge）触发战场脉冲——把经济动作变成武器
- 当前 overview：每一次合成都是一发炮弹。

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.fateLoom.name` | 命运织机 |
| `cards.fateLoom.overview` | 每一次合成都是一发炮弹。 |
| `cards.fateLoom.hand.shortByTier.1` | 合成时释放一次脉冲伤害 |
| `cards.fateLoom.hand.shortByTier.3` | 合成脉冲加增伤区，覆盖更广 |
| `cards.fateLoom.hand.shortByTier.6` | 每波自动脉冲，不依赖合成频率 |
| `cards.fateLoom.hand.milestones.3` | **命运织机强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.fateLoom.hand.milestones.6` | **命运织机终态** / 解锁公共 6★ 终极形态。 （fx: transform） |
| `cards.fateLoom.equip.shortByTier.3` | 已选分支：织机 / 织网 / 波末织机 |
| `cards.fateLoom.equip.shortByTier.5` | 叠加接口：命运兑现 / 织网扩张 / 命运转化 |
| `cards.fateLoom.equip.shortByTier.6` | 终极形态：合成输出最大化 |
| `cards.fateLoom.equip.milestones.3` | **命运织机强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.fateLoom.equip.milestones.5` | **命运织机再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.fateLoom.equip.milestones.6` | **命运织机终态** / 解锁公共 6★ 终极形态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；100% 概率额外生成 1 个掉落物
- **3★**：半径 140；生成半径 145 的光环，每 0.6 秒 结算；优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120；100% 概率额外生成 3 个掉落物
- **6★**：半径 160；生成半径 145 的光环，每 0.6 秒 结算；优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120；指定属性提高 116%，持续 5 秒，最多 4 层；100% 概率额外生成 5 个掉落物

### 3★ 分支（三选一）

#### `fateLoomA` — 当前分支名「织机」

精确效果：
- 完成合成时 → 合成时释放半径 180 的脉冲，每次累计合成造成 5 点伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.fateLoom.fateLoomA.name` | 织机 |
| `evolution.fateLoom.fateLoomA.summary` | 每次合成时向场上释放一次脉冲伤害 |
| `evolution.fateLoom.fateLoomA.intent` | 核心机制：合成触发范围脉冲伤害 |

#### `fateLoomB` — 当前分支名「织网」

精确效果：
- 完成合成时 → 合成时释放半径 210 的脉冲，每次累计合成造成 7 点伤害；生成半径 145 的光环，每 0.6 秒 结算；　优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.fateLoom.fateLoomB.name` | 织网 |
| `evolution.fateLoom.fateLoomB.summary` | 合成时脉冲范围扩大，并留下短时增伤区域 |
| `evolution.fateLoom.fateLoomB.intent` | 核心机制：合成触发扩大脉冲+增伤光环 |

#### `fateLoomC` — 当前分支名「波末织机」

精确效果：
- 每波开始时 → 合成时释放半径 240 的脉冲，每次累计合成造成 9 点伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.fateLoom.fateLoomC.name` | 波末织机 |
| `evolution.fateLoom.fateLoomC.summary` | 每波开始时自动触发一次脉冲，不依赖合成频率 |
| `evolution.fateLoom.fateLoomC.intent` | 核心机制：波次触发自动脉冲，与玩家操作解耦 |

### 5★ 分支（三选一）

#### `fateLoom1x` — 当前分支名「命运兑现」　接口角色：payoff

精确效果：
- 击杀追求者时（目标同时处于标记状态） → 造成 0.6 倍爆发伤害，作用半径 95；22% 概率额外生成 1 个掉落物

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.fateLoom.fateLoom1x.name` | 命运兑现 |
| `evolution.fateLoom.fateLoom1x.summary` | 有赏印的追求者被击杀时，爆发并偶尔多掉心意 |
| `evolution.fateLoom.fateLoom1x.intent` | 兑现接口：赏印击杀触发爆发+额外心意产出 |

#### `fateLoom2x` — 当前分支名「织网扩张」　接口角色：spread

精确效果：
- 完成合成时 → 生成半径 145 的光环，每 0.6 秒 结算；　优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.fateLoom.fateLoom2x.name` | 织网扩张 |
| `evolution.fateLoom.fateLoom2x.summary` | 每次合成后，全场短时展开增伤光环 |
| `evolution.fateLoom.fateLoom2x.intent` | 传播接口：合成触发全场增伤扩散 |

#### `fateLoom3x` — 当前分支名「命运转化」　接口角色：convert

精确效果：
- 持续生效 → Bounty 或波末 Boss发放万能卡奖励时，有 20% 概率额外增加 1 张
- 完成合成时 → 伤害提高 105%，持续 2 秒，最多 3 层

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.fateLoom.fateLoom3x.name` | 命运转化 |
| `evolution.fateLoom.fateLoom3x.summary` | 每次合成后，短时提升伤害，并提高万能卡奖励获取概率 |
| `evolution.fateLoom.fateLoom3x.intent` | 转化接口：合成触发属性提升+万能卡奖励加成 |

### 4★ 公共节点

- 数值放大：{"damagePerMergeCount":"+25"}

### 6★ 公共节点

- 完成合成时 → 合成时释放半径 240 的脉冲，每次累计合成造成 9 点伤害；指定属性提高 116%，持续 5 秒，最多 4 层；掉落概率 +22%；掉落物存在时间 +31%；奖励积分获取 +22%

---

## `goldenVolley` — 当前名「鎏金弹」

- 神：丰饶（`plenty`）　类别：弹道　标签：projectile, utility
- 身份契约：弹道载体逐个命中逐个铺设赏印
- 当前 overview：每一发都在铸赏印——子弹就是货币。

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.goldenVolley.name` | 鎏金弹 |
| `cards.goldenVolley.overview` | 每一发都在铸赏印——子弹就是货币。 |
| `cards.goldenVolley.hand.shortByTier.1` | 穿透，每个穿透目标被标记赏印 |
| `cards.goldenVolley.hand.shortByTier.3` | 命中分裂三弹片，弹片各自标记 |
| `cards.goldenVolley.hand.shortByTier.6` | 光束持续标记路径上所有追求者 |
| `cards.goldenVolley.hand.milestones.3` | **鎏金弹强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.goldenVolley.hand.milestones.6` | **鎏金弹终态** / 解锁公共 6★ 终极形态。 （fx: transform） |
| `cards.goldenVolley.equip.shortByTier.3` | 已选分支：鎏金弹 / 金雨 / 定点鎏金 |
| `cards.goldenVolley.equip.shortByTier.5` | 叠加接口：赏金兑现 / 印记扩散 / 鎏金回收 |
| `cards.goldenVolley.equip.shortByTier.6` | 终极形态：标记+兑现最大化 |
| `cards.goldenVolley.equip.milestones.3` | **鎏金弹强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.goldenVolley.equip.milestones.5` | **鎏金弹再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.goldenVolley.equip.milestones.6` | **鎏金弹终态** / 解锁公共 6★ 终极形态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；弹道穿透 2 个目标，每次保留 78% 伤害；100% 概率额外生成 1 个掉落物
- **3★**：半径 140；优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；弹道穿透 2 个目标，每次保留 78% 伤害；分裂出 3 枚弹道，造成 50% 伤害，最多递归 1 层；100% 概率额外生成 3 个掉落物
- **6★**：半径 160；优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；弹道穿透 2 个目标，每次保留 78% 伤害；分裂出 3 枚弹道，造成 50% 伤害，最多递归 1 层；主炮变为光束，每 0.8 秒发射，宽度 36，每次造成 90% 伤害；100% 概率额外生成 5 个掉落物

### 3★ 分支（三选一）

#### `goldenVolleyA` — 当前分支名「鎏金弹」

精确效果：
- 每次开火时 → 优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；弹道穿透 2 个目标，每次保留 78% 伤害
- 命中追求者时 → 优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100
- 每次开火时 → 弹道穿透 2 个目标，每次保留 78% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.goldenVolley.goldenVolleyA.name` | 鎏金弹 |
| `evolution.goldenVolley.goldenVolleyA.summary` | 子弹穿透，每个被穿过的追求者都被标记赏印 |
| `evolution.goldenVolley.goldenVolleyA.intent` | 核心机制：开火触发穿透，逐个施加赏印 |

#### `goldenVolleyB` — 当前分支名「金雨」

精确效果：
- 命中追求者时 → 优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120；分裂出 3 枚弹道，造成 50% 伤害，最多递归 1 层

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.goldenVolley.goldenVolleyB.name` | 金雨 |
| `evolution.goldenVolley.goldenVolleyB.summary` | 命中后分裂出三枚弹片，弹片各自标记追求者 |
| `evolution.goldenVolley.goldenVolleyB.intent` | 核心机制：命中触发分裂，子弹道独立施加赏印 |

#### `goldenVolleyC` — 当前分支名「定点鎏金」

精确效果：
- 持续生效 → 优先锁定指定目标，索敌权重 2，持续 5 秒，作用半径 140；主炮变为光束，每 0.8 秒发射，宽度 36，每次造成 90% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.goldenVolley.goldenVolleyC.name` | 定点鎏金 |
| `evolution.goldenVolley.goldenVolleyC.summary` | 主炮换形为光束，路径追求者持续被标记赏印 |
| `evolution.goldenVolley.goldenVolleyC.intent` | 核心机制：光束变形，持续赏印覆盖 |

### 5★ 分支（三选一）

#### `goldenVolley1x` — 当前分支名「赏金兑现」　接口角色：payoff

精确效果：
- 命中追求者时（目标同时处于标记状态） → 造成 0.8 倍爆发伤害，作用半径 85
- 命中追求者时 → 伤害提高 103%，持续 1.5 秒，最多 1 层

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.goldenVolley.goldenVolley1x.name` | 赏金兑现 |
| `evolution.goldenVolley.goldenVolley1x.summary` | 有赏印的追求者被命中时，触发爆发（穿透各自独立结算） |
| `evolution.goldenVolley.goldenVolley1x.intent` | 兑现接口：赏印命中触发爆发，穿透并发逐个结算 |

#### `goldenVolley2x` — 当前分支名「印记扩散」　接口角色：spread

精确效果：
- 命中追求者时（目标同时处于标记状态） → 命中处引发范围爆发，半径 85，中心造成 55% 伤害，边缘衰减至 40%；优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120
- 命中追求者时 → 伤害提高 103%，持续 1.5 秒，最多 1 层

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.goldenVolley.goldenVolley2x.name` | 印记扩散 |
| `evolution.goldenVolley.goldenVolley2x.summary` | 有赏印的追求者被命中时，范围内传播赏印 |
| `evolution.goldenVolley.goldenVolley2x.intent` | 传播接口：赏印命中触发范围赏印扩散 |

#### `goldenVolley3x` — 当前分支名「鎏金回收」　接口角色：convert

精确效果：
- 击杀追求者时（目标同时处于标记状态） → 45% 概率额外生成 1 个掉落物
- 击杀追求者时 → 伤害提高 103%，持续 1.5 秒，最多 1 层

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.goldenVolley.goldenVolley3x.name` | 鎏金回收 |
| `evolution.goldenVolley.goldenVolley3x.summary` | 有赏印的追求者被击杀时，多掉一份心意 |
| `evolution.goldenVolley.goldenVolley3x.intent` | 转化接口：赏印击杀触发额外心意产出 |

### 4★ 公共节点

- 数值放大：{"damageRetention":"+1","width":"+6"}

### 6★ 公共节点

- 持续生效 → 优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；掉落概率 +12%；掉落物存在时间 +15%；奖励积分获取 +12%

---

## `bountyCall` — 当前名「富集号令」

- 神：丰饶（`plenty`）　类别：控制　标签：control, utility
- 身份契约：主动点名：批量施加赏印并引导集火
- 当前 overview：用标记指挥优先级——控制掉落节奏。

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.bountyCall.name` | 富集号令 |
| `cards.bountyCall.overview` | 用标记指挥优先级——控制掉落节奏。 |
| `cards.bountyCall.hand.shortByTier.1` | 每波批量标记高价值追求者 |
| `cards.bountyCall.hand.shortByTier.3` | 周期滚动标记，持续维持赏印覆盖 |
| `cards.bountyCall.hand.shortByTier.6` | 拾取驱动叠加，拾取节奏决定赏印数量 |
| `cards.bountyCall.hand.milestones.3` | **富集号令强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.bountyCall.hand.milestones.6` | **富集号令终态** / 解锁公共 6★ 终极形态。 （fx: transform） |
| `cards.bountyCall.equip.shortByTier.3` | 已选分支：号令 / 广域号令 / 拾取号令 |
| `cards.bountyCall.equip.shortByTier.5` | 叠加接口：悬赏兑现 / 号令扩散 / 号令转化 |
| `cards.bountyCall.equip.shortByTier.6` | 终极形态：赏印统治最大化 |
| `cards.bountyCall.equip.milestones.3` | **富集号令强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.bountyCall.equip.milestones.5` | **富集号令再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.bountyCall.equip.milestones.6` | **富集号令终态** / 解锁公共 6★ 终极形态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；优先锁定指定目标，索敌权重 1.4，目标生命阈值 100%，持续 3 秒，作用半径 100；100% 概率额外生成 1 个掉落物
- **3★**：半径 140；优先锁定指定目标，索敌权重 1.4，目标生命阈值 100%，持续 3 秒，作用半径 100；100% 概率额外生成 3 个掉落物
- **6★**：半径 160；优先锁定指定目标，索敌权重 1.4，目标生命阈值 100%，持续 3 秒，作用半径 100；攻速提高 112%，持续 4 秒，最多 3 层；100% 概率额外生成 5 个掉落物

### 3★ 分支（三选一）

#### `bountyCallA` — 当前分支名「号令」

精确效果：
- 每波开始时 → 优先锁定指定目标，索敌权重 1.4，目标生命阈值 100%，持续 3 秒，作用半径 100

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.bountyCall.bountyCallA.name` | 号令 |
| `evolution.bountyCall.bountyCallA.summary` | 每波开始标记若干高价值追求者 |
| `evolution.bountyCall.bountyCallA.intent` | 核心机制：波次触发批量赏印 |

#### `bountyCallB` — 当前分支名「广域号令」

精确效果：
- 每 3 秒 → 优先锁定指定目标，索敌权重 1.85，持续 4 秒，作用半径 115
- 命中追求者时（目标同时处于标记状态） → 造成 0.22 倍爆发伤害，作用半径 0

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.bountyCall.bountyCallB.name` | 广域号令 |
| `evolution.bountyCall.bountyCallB.summary` | 每 3 秒滚动标记多个目标，持续维持赏印覆盖 |
| `evolution.bountyCall.bountyCallB.intent` | 核心机制：周期触发持续多目标赏印 |

#### `bountyCallC` — 当前分支名「拾取号令」

精确效果：
- 拾取掉落时 → 优先锁定指定目标，索敌权重 2，持续 4 秒，作用半径 65

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.bountyCall.bountyCallC.name` | 拾取号令 |
| `evolution.bountyCall.bountyCallC.summary` | 每次拾取心意后立刻追加标记，拾取节奏决定赏印数量 |
| `evolution.bountyCall.bountyCallC.intent` | 核心机制：拾取触发赏印叠加 |

### 5★ 分支（三选一）

#### `bountyCall1x` — 当前分支名「悬赏兑现」　接口角色：payoff

精确效果：
- 击杀追求者时（目标同时处于标记状态） → 造成 0.65 倍爆发伤害，作用半径 90；优先锁定指定目标，索敌权重 2，持续 5 秒，作用半径 100；20% 概率额外生成 1 个掉落物

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.bountyCall.bountyCall1x.name` | 悬赏兑现 |
| `evolution.bountyCall.bountyCall1x.summary` | 有赏印的追求者被击杀时，爆发并偶尔多掉心意 |
| `evolution.bountyCall.bountyCall1x.intent` | 兑现接口：赏印击杀触发爆发+额外心意产出 |

#### `bountyCall2x` — 当前分支名「号令扩散」　接口角色：spread

精确效果：
- 击杀追求者时（目标同时处于标记状态） → 生成半径 145 的光环，每 0.6 秒 结算；　优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120
- 击杀追求者时 → 伤害提高 103%，持续 1.5 秒，最多 1 层

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.bountyCall.bountyCall2x.name` | 号令扩散 |
| `evolution.bountyCall.bountyCall2x.summary` | 有赏印的追求者被击杀时，死亡位留下短时赏印光环 |
| `evolution.bountyCall.bountyCall2x.intent` | 传播接口：赏印击杀触发范围赏印扩散 |

#### `bountyCall3x` — 当前分支名「号令转化」　接口角色：convert

精确效果：
- 持续生效 → 掉落概率 +22%；奖励积分获取 +22%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.bountyCall.bountyCall3x.name` | 号令转化 |
| `evolution.bountyCall.bountyCall3x.summary` | 持续提高心意掉落率和奖励积分获取 |
| `evolution.bountyCall.bountyCall3x.intent` | 转化接口：被动掉落率提升+积分获取加成 |

### 4★ 公共节点

- 数值放大：{"hpThresholdRatio":"+0.2"}

### 6★ 公共节点

- 每波开始时 → 优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120；攻速提高 112%，持续 4 秒，最多 3 层；掉落概率 +17%；掉落物存在时间 +23%；奖励积分获取 +17%

---

## `overgrowth` — 当前名「蔓生」

- 神：丰饶（`plenty`）　类别：领域　标签：domain, control
- 身份契约：常驻蔓生领域：范围伤害 + 延长掉落存续
- 当前 overview：把整片领地变成收获田。

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.overgrowth.name` | 蔓生 |
| `cards.overgrowth.overview` | 把整片领地变成收获田。 |
| `cards.overgrowth.hand.shortByTier.1` | 常驻赏印光环，圈内掉落存续更久 |
| `cards.overgrowth.hand.shortByTier.3` | 周期扩张蔓生环，覆盖更远 |
| `cards.overgrowth.hand.shortByTier.6` | 每波强化蔓生，延长心意时限 |
| `cards.overgrowth.hand.milestones.3` | **蔓生强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.overgrowth.hand.milestones.6` | **蔓生终态** / 解锁公共 6★ 终极形态。 （fx: transform） |
| `cards.overgrowth.equip.shortByTier.3` | 已选分支：蔓生 / 蔓延 / 波末蔓生 |
| `cards.overgrowth.equip.shortByTier.5` | 叠加接口：蔓生绞杀 / 蔓延传播 / 蔓生丰收 |
| `cards.overgrowth.equip.shortByTier.6` | 终极形态：蔓生经济最大化 |
| `cards.overgrowth.equip.milestones.3` | **蔓生强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.overgrowth.equip.milestones.5` | **蔓生再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.overgrowth.equip.milestones.6` | **蔓生终态** / 解锁公共 6★ 终极形态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；生成光环，半径为射程的 50%，每 0.6 秒 结算；优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；100% 概率额外生成 1 个掉落物
- **3★**：半径 140；生成光环，半径为射程的 50%，每 0.6 秒 结算；优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；100% 概率额外生成 3 个掉落物
- **6★**：半径 160；生成光环，半径为射程的 50%，每 0.6 秒 结算；优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；100% 概率额外生成 5 个掉落物

### 3★ 分支（三选一）

#### `overgrowthA` — 当前分支名「蔓生」

精确效果：
- 持续生效 → 生成光环，半径为射程的 50%，每 0.6 秒 结算；　优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100
- 每 3 秒 → 优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.overgrowth.overgrowthA.name` | 蔓生 |
| `evolution.overgrowth.overgrowthA.summary` | 炮台常驻蔓生光环，圈内追求者被标记，心意掉落存续更久 |
| `evolution.overgrowth.overgrowthA.intent` | 核心机制：被动光环，持续赏印+心意时限延长 |

#### `overgrowthB` — 当前分支名「蔓延」

精确效果：
- 每 4 秒 → 生成半径 85 的光环，每 0.6 秒 结算；　优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120
- 每 4 秒 → 优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.overgrowth.overgrowthB.name` | 蔓延 |
| `evolution.overgrowth.overgrowthB.summary` | 每 4 秒向外推出扩张蔓生环，覆盖更远追求者 |
| `evolution.overgrowth.overgrowthB.intent` | 核心机制：周期触发扩张型赏印光环 |

#### `overgrowthC` — 当前分支名「波末蔓生」

精确效果：
- 每波开始时 → 掉落物存在时间 +31%；生成半径 165 的光环，每 0.6 秒 结算；　优先锁定指定目标，索敌权重 2，持续 5 秒，作用半径 140

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.overgrowth.overgrowthC.name` | 波末蔓生 |
| `evolution.overgrowth.overgrowthC.summary` | 每波开始建立强化蔓生，延长本波心意掉落存续时间 |
| `evolution.overgrowth.overgrowthC.intent` | 核心机制：波次触发宽范围赏印光环+心意时限 |

### 5★ 分支（三选一）

#### `overgrowth1x` — 当前分支名「蔓生绞杀」　接口角色：payoff

精确效果：
- 每 3 秒 → 造成 0.65 倍爆发伤害，作用半径 85

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.overgrowth.overgrowth1x.name` | 蔓生绞杀 |
| `evolution.overgrowth.overgrowth1x.summary` | 每 3 秒对场上所有有赏印的追求者各触发一次爆发 |
| `evolution.overgrowth.overgrowth1x.intent` | 兑现接口：定时对赏印目标触发逐点爆发 |

#### `overgrowth2x` — 当前分支名「蔓延传播」　接口角色：spread

精确效果：
- 拾取掉落时 → 生成半径 100 的光环，每 0.65 秒 结算；　优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 100

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.overgrowth.overgrowth2x.name` | 蔓延传播 |
| `evolution.overgrowth.overgrowth2x.summary` | 每次拾取心意后，短时扩展蔓生光环范围 |
| `evolution.overgrowth.overgrowth2x.intent` | 传播接口：拾取触发光环范围扩张 |

#### `overgrowth3x` — 当前分支名「蔓生丰收」　接口角色：convert

精确效果：
- 持续生效 → 掉落物存在时间 +32%
- 拾取掉落时 → 恢复最大生命的 7%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.overgrowth.overgrowth3x.name` | 蔓生丰收 |
| `evolution.overgrowth.overgrowth3x.summary` | 心意在地上存续更久，且每次拾取时回复少量心防 |
| `evolution.overgrowth.overgrowth3x.intent` | 转化接口：心意时限延长+拾取触发心防回复 |

### 4★ 公共节点

- 数值放大：{"radiusRatioOfRange":"*1.1"}

### 6★ 公共节点

- 持续生效 → 掉落概率 +22%；生成半径 165 的光环，每 0.6 秒 结算；　优先锁定指定目标，索敌权重 2，持续 5 秒，作用半径 140；掉落物存在时间 +31%；奖励积分获取 +22%

---

## `springOfLife` — 当前名「生命涌泉」

- 神：丰饶（`plenty`）　类别：防御　标签：defense, utility
- 身份契约：丰饶的防御＝资源转化：拾取与赏印事件直接变成回复（不使用 shield，归磐垒）
- 当前 overview：拾取心意，心防就回来了。

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.springOfLife.name` | 生命涌泉 |
| `cards.springOfLife.overview` | 拾取心意，心防就回来了。 |
| `cards.springOfLife.hand.shortByTier.1` | 每次拾取心意时回复少量心防 |
| `cards.springOfLife.hand.shortByTier.3` | 常驻泉域赏印，每 4 秒自动回复心防 |
| `cards.springOfLife.hand.shortByTier.6` | 每波大量回复心防并提升生命上限 |
| `cards.springOfLife.hand.milestones.3` | **生命涌泉强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.springOfLife.hand.milestones.6` | **生命涌泉终态** / 解锁公共 6★ 终极形态。 （fx: transform） |
| `cards.springOfLife.equip.shortByTier.3` | 已选分支：涌泉 / 泉域 / 波末涌泉 |
| `cards.springOfLife.equip.shortByTier.5` | 叠加接口：涌泉爆发 / 泉水蔓延 / 过量转化 |
| `cards.springOfLife.equip.shortByTier.6` | 终极形态：拾取回复最大化 |
| `cards.springOfLife.equip.milestones.3` | **生命涌泉强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.springOfLife.equip.milestones.5` | **生命涌泉再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.springOfLife.equip.milestones.6` | **生命涌泉终态** / 解锁公共 6★ 终极形态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；恢复最大生命的 3%；优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；100% 概率额外生成 1 个掉落物
- **3★**：半径 140；恢复最大生命的 3%；优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；生成半径 145 的光环，每 0.6 秒 结算；优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120；100% 概率额外生成 3 个掉落物
- **6★**：半径 160；恢复最大生命的 3%；优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100；生成半径 145 的光环，每 0.6 秒 结算；优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120；指定属性提高 116%，持续 5 秒，最多 4 层；100% 概率额外生成 5 个掉落物

### 3★ 分支（三选一）

#### `springOfLifeA` — 当前分支名「涌泉」

精确效果：
- 拾取掉落时 → 恢复最大生命的 3%
- 每 3 秒 → 优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.springOfLife.springOfLifeA.name` | 涌泉 |
| `evolution.springOfLife.springOfLifeA.summary` | 每次拾取心意时回复少量心防 |
| `evolution.springOfLife.springOfLifeA.intent` | 核心机制：拾取触发心防回复 |

#### `springOfLifeB` — 当前分支名「泉域」

精确效果：
- 持续生效 → 恢复最大生命的 5%；生成半径 145 的光环，每 0.6 秒 结算；　优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120
- 每 4 秒 → 恢复最大生命的 5%；生成半径 145 的光环，每 0.6 秒 结算；　优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120
- 每 4 秒 → 优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.springOfLife.springOfLifeB.name` | 泉域 |
| `evolution.springOfLife.springOfLifeB.summary` | 常驻泉域，圈内追求者被标记，每 4 秒自动回复心防 |
| `evolution.springOfLife.springOfLifeB.intent` | 核心机制：被动光环赏印+周期心防回复 |

#### `springOfLifeC` — 当前分支名「波末涌泉」

精确效果：
- 每波开始时 → 指定属性提高 116%，持续 5 秒，最多 4 层；恢复最大生命的 7%
- 每 5 秒 → 优先锁定指定目标，索敌权重 2，持续 5 秒，作用半径 140

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.springOfLife.springOfLifeC.name` | 波末涌泉 |
| `evolution.springOfLife.springOfLifeC.summary` | 每波开始大幅回复心防并提升生命上限 |
| `evolution.springOfLife.springOfLifeC.intent` | 核心机制：波次触发大量心防回复+生命上限提升 |

### 5★ 分支（三选一）

#### `springOfLife1x` — 当前分支名「涌泉爆发」　接口角色：payoff

精确效果：
- 击杀追求者时（目标同时处于标记状态） → 恢复最大生命的 3%
- 击杀追求者时 → 伤害提高 103%，持续 1.5 秒，最多 1 层

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.springOfLife.springOfLife1x.name` | 涌泉爆发 |
| `evolution.springOfLife.springOfLife1x.summary` | 有赏印的追求者被击杀时，回复少量心防 |
| `evolution.springOfLife.springOfLife1x.intent` | 兑现接口：赏印击杀触发心防回复 |

#### `springOfLife2x` — 当前分支名「泉水蔓延」　接口角色：spread

精确效果：
- 拾取掉落时 → 生成半径 80 的光环，每 0.45 秒 结算；　恢复最大生命的 6%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.springOfLife.springOfLife2x.name` | 泉水蔓延 |
| `evolution.springOfLife.springOfLife2x.summary` | 每次拾取心意后，短时展开范围心防回复光环 |
| `evolution.springOfLife.springOfLife2x.intent` | 传播接口：拾取触发范围心防回复扩散 |

#### `springOfLife3x` — 当前分支名「过量转化」　接口角色：convert

精确效果：
- 持续生效 → 掉落物过期时，将其中 31% 转化为收益

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.springOfLife.springOfLife3x.name` | 过量转化 |
| `evolution.springOfLife.springOfLife3x.summary` | 心意过期时，将部分价值转化为心防回复 |
| `evolution.springOfLife.springOfLife3x.intent` | 转化接口：心意过期驱动心防回复 |

### 4★ 公共节点

- 数值放大：{"priorityWeight":"+1"}

### 6★ 公共节点

- 每波开始时 → 伤害提高 108%，持续 3 秒，最多 2 层；恢复最大生命的 3%；掉落概率 +12%；掉落物存在时间 +15%；奖励积分获取 +12%
- 每 2.5 秒 → 优先锁定指定目标，索敌权重 1.4，持续 3 秒，作用半径 100

---

## `luckyStar` — 当前名「吉星」

- 神：丰饶（`plenty`）　类别：经济　标签：utility
- 身份契约：掉落规则的乘数中心：掉率/经验/连锁拾取
- 当前 overview：运气也是可以装备的。

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.luckyStar.name` | 吉星 |
| `cards.luckyStar.overview` | 运气也是可以装备的。 |
| `cards.luckyStar.hand.shortByTier.1` | 提高心意掉落率和奖励积分获取 |
| `cards.luckyStar.hand.shortByTier.3` | 星域每 5 秒直接产出心意 |
| `cards.luckyStar.hand.shortByTier.6` | 拾取后有概率再掉一份，形成连锁 |
| `cards.luckyStar.hand.milestones.3` | **吉星强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.luckyStar.hand.milestones.6` | **吉星终态** / 解锁公共 6★ 终极形态。 （fx: transform） |
| `cards.luckyStar.equip.shortByTier.3` | 已选分支：吉星 / 星域 / 拾取吉星 |
| `cards.luckyStar.equip.shortByTier.5` | 叠加接口：吉星兑现 / 星运扩散 / 转运 |
| `cards.luckyStar.equip.shortByTier.6` | 终极形态：经济最大化 |
| `cards.luckyStar.equip.milestones.3` | **吉星强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.luckyStar.equip.milestones.5` | **吉星再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.luckyStar.equip.milestones.6` | **吉星终态** / 解锁公共 6★ 终极形态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；100% 概率额外生成 1 个掉落物
- **3★**：半径 140；35% 概率额外生成 1 个掉落物；优先锁定指定目标，索敌权重 1.7，目标生命阈值 100%，持续 4 秒，作用半径 120
- **6★**：半径 160；35% 概率额外生成 1 个掉落物；优先锁定指定目标，索敌权重 1.7，目标生命阈值 100%，持续 4 秒，作用半径 120

### 3★ 分支（三选一）

#### `luckyStarA` — 当前分支名「吉星」

精确效果：
- 持续生效 → 掉落概率 +12%；奖励积分获取 +12%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.luckyStar.luckyStarA.name` | 吉星 |
| `evolution.luckyStar.luckyStarA.summary` | 持续提高心意掉落率和奖励积分获取 |
| `evolution.luckyStar.luckyStarA.intent` | 核心机制：被动双向经济属性提升 |

#### `luckyStarB` — 当前分支名「星域」

精确效果：
- 每 5 秒 → 35% 概率额外生成 1 个掉落物
- 每 4 秒 → 优先锁定指定目标，索敌权重 1.7，目标生命阈值 100%，持续 4 秒，作用半径 120

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.luckyStar.luckyStarB.name` | 星域 |
| `evolution.luckyStar.luckyStarB.summary` | 每 5 秒在最密集追求者处直接掉落心意 |
| `evolution.luckyStar.luckyStarB.intent` | 核心机制：周期触发主动心意产出 |

#### `luckyStarC` — 当前分支名「拾取吉星」

精确效果：
- 拾取掉落时 → 45% 概率额外生成 1 个掉落物
- 每 5 秒 → 优先锁定指定目标，索敌权重 2，持续 5 秒，作用半径 140

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.luckyStar.luckyStarC.name` | 拾取吉星 |
| `evolution.luckyStar.luckyStarC.summary` | 每次拾取心意后有概率再掉一份，形成拾取连锁 |
| `evolution.luckyStar.luckyStarC.intent` | 核心机制：拾取触发额外产出连锁 |

### 5★ 分支（三选一）

#### `luckyStar1x` — 当前分支名「吉星兑现」　接口角色：payoff

精确效果：
- 击杀追求者时（目标同时处于标记状态） → 65% 概率额外生成 1 个掉落物，星级按配置权重抽取

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.luckyStar.luckyStar1x.name` | 吉星兑现 |
| `evolution.luckyStar.luckyStar1x.summary` | 有赏印的追求者被击杀时，高概率多掉一份心意 |
| `evolution.luckyStar.luckyStar1x.intent` | 兑现接口：赏印击杀触发高概率额外产出 |

#### `luckyStar2x` — 当前分支名「星运扩散」　接口角色：spread

精确效果：
- 完成合成时 → 优先锁定指定目标，索敌权重 1.7，持续 4 秒，作用半径 120；合成时释放半径 210 的脉冲，每次累计合成造成 7 点伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.luckyStar.luckyStar2x.name` | 星运扩散 |
| `evolution.luckyStar.luckyStar2x.summary` | 每次合成后，释放一次脉冲并追加一个赏印标记 |
| `evolution.luckyStar.luckyStar2x.intent` | 传播接口：合成触发脉冲+赏印扩增 |

#### `luckyStar3x` — 当前分支名「转运」　接口角色：convert

精确效果：
- 持续生效 → 掉落物过期时，将其中 42% 转化为收益；奖励积分获取 +20%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.luckyStar.luckyStar3x.name` | 转运 |
| `evolution.luckyStar.luckyStar3x.summary` | 心意过期时按比例转化为收益，并持续提高积分获取 |
| `evolution.luckyStar.luckyStar3x.intent` | 转化接口：过期价值转化+积分倍增 |

### 4★ 公共节点

- 数值放大：{"hpThresholdRatio":"+0.1"}

### 6★ 公共节点

- 持续生效 → 掉落概率 +17%；奖励积分获取 +17%；掉落物存在时间 +23%
