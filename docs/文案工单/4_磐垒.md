# ProjectVL 文案工单 · 磐垒

> 由 `scripts/exportCopyWorkOrder.ts` 生成；机制句与游戏内「精确效果」显示一致。表格里的 JSON 路径即 `src/data/texts.json` 写入位置。

---

## `aegis` — 当前名「神盾」

- 神：磐垒（`bulwark`）　类别：防御　标签：defense
- 身份契约：供应壁垒容量（全表唯一 shield 来源），破盾时新星反击
- 当前 overview：壁垒的唯一供应商。

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.aegis.name` | 神盾 |
| `cards.aegis.overview` | 壁垒的唯一供应商。 |
| `cards.aegis.hand.shortByTier.1` | 核心载体 · 拖放释放 |
| `cards.aegis.hand.shortByTier.3` | 核心载体强化 · 拖放释放 |
| `cards.aegis.hand.shortByTier.6` | 公共终态 · 拖放释放 |
| `cards.aegis.hand.milestones.3` | **神盾强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.aegis.hand.milestones.6` | **神盾终态** / 解锁公共 6★ 终态。 （fx: transform） |
| `cards.aegis.equip.shortByTier.3` | 选择 护盾/再生盾/应激盾 |
| `cards.aegis.equip.shortByTier.5` | 叠加兑现/传播/转化分支 |
| `cards.aegis.equip.shortByTier.6` | 公共终态 · 终态 |
| `cards.aegis.equip.milestones.3` | **神盾强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.aegis.equip.milestones.5` | **神盾再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.aegis.equip.milestones.6` | **神盾终态** / 解锁公共 6★ 终态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；获得可抵挡 2 次伤害的护盾
- **3★**：半径 140；获得可抵挡 4 次伤害的护盾
- **6★**：半径 160；获得可抵挡 6 次伤害的护盾，每 8 秒 恢复

### 3★ 分支（三选一）

#### `aegisA` — 当前分支名「护盾」

精确效果：
- 每波开始时 → 获得可抵挡 2 次伤害的护盾，每 9 秒 恢复

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.aegis.aegisA.name` | 护盾 |
| `evolution.aegis.aegisA.summary` | 每波开始获得 2 次吸收的壁垒 |
| `evolution.aegis.aegisA.intent` | 护盾以shield(absorbHits=2) @ onWaveStart承担核心机制。 |
| `evolution.aegis.aegisA.keywords` | shield(absorbHits=2) @ onWaveStart |
| `evolution.aegis.aegisA.buildFit` | 每波开始获得 2 次吸收的壁垒 |

#### `aegisB` — 当前分支名「再生盾」

精确效果：
- 每波开始时 → 获得可抵挡 1 次伤害的护盾，每 8 秒 恢复
- 每次开火时 → 弹道穿透 1 个目标，每次保留 90% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.aegis.aegisB.name` | 再生盾 |
| `evolution.aegis.aegisB.summary` | 容量低但会自行再生 |
| `evolution.aegis.aegisB.intent` | 再生盾以shield(absorbHits=1, regenSeconds=8) @ onWaveStart承担核心机制。 |
| `evolution.aegis.aegisB.keywords` | shield(absorbHits=1, regenSeconds=8) @ onWaveStart |
| `evolution.aegis.aegisB.buildFit` | 容量低但会自行再生 |

#### `aegisC` — 当前分支名「应激盾」

精确效果：
- 每 9 秒 → 获得可抵挡 3 次伤害的护盾，每 7 秒 恢复

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.aegis.aegisC.name` | 应激盾 |
| `evolution.aegis.aegisC.summary` | 周期补盾，不依赖波次节奏 |
| `evolution.aegis.aegisC.intent` | 应激盾以shield @ interval 9s承担核心机制。 |
| `evolution.aegis.aegisC.keywords` | shield @ interval 9s |
| `evolution.aegis.aegisC.buildFit` | 周期补盾，不依赖波次节奏 |

### 5★ 分支（三选一）

#### `aegis1x` — 当前分支名「破盾新星」　接口角色：payoff

精确效果：
- 持续生效 → 护盾破裂时造成 12 点范围伤害并击退 45 距离

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.aegis.aegis1x.name` | 破盾新星 |
| `evolution.aegis.aegis1x.summary` | passive → novaOnBreak（全表唯一持有者）破盾时对周围伤害并击退 |
| `evolution.aegis.aegis1x.intent` | 兑现接口：passive → novaOnBreak（全表唯一持有者）破盾时对周围伤害并击退 |
| `evolution.aegis.aegis1x.keywords` | 兑现 |
| `evolution.aegis.aegis1x.buildFit` | passive → novaOnBreak（全表唯一持有者）破盾时对周围伤害并击退 |

#### `aegis2x` — 当前分支名「盾域」　接口角色：spread

精确效果：
- 持续生效 → 生成半径 145 的光环，每 0.7 秒 结算；　敌人突破造成的伤害降低 12%；　召唤 1 个诱饵，生命 70，持续 6 秒

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.aegis.aegis2x.name` | 盾域 |
| `evolution.aegis.aegis2x.summary` | passive → aura 减伤光环，范围内召唤物同享保护 |
| `evolution.aegis.aegis2x.intent` | 传播接口：passive → aura 减伤光环，范围内召唤物同享保护 |
| `evolution.aegis.aegis2x.keywords` | 传播 |
| `evolution.aegis.aegis2x.buildFit` | passive → aura 减伤光环，范围内召唤物同享保护 |

#### `aegis3x` — 当前分支名「盾能转化」　接口角色：convert

精确效果：
- 每波开始时 → 指定属性提高 116%，持续 5 秒，最多 4 层；恢复最大生命的 7%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.aegis.aegis3x.name` | 盾能转化 |
| `evolution.aegis.aegis3x.summary` | onWaveStart → 若上一波未破盾，则 restore + statBuff |
| `evolution.aegis.aegis3x.intent` | 转化接口：onWaveStart → 若上一波未破盾，则 restore + statBuff |
| `evolution.aegis.aegis3x.keywords` | 转化 |
| `evolution.aegis.aegis3x.buildFit` | onWaveStart → 若上一波未破盾，则 restore + statBuff |

### 4★ 公共节点

- 数值放大：{"absorbHits":"+1","regenSeconds":"-2"}

### 6★ 公共节点

- 持续生效 → 敌人突破造成的伤害降低 8%

---

## `thorns` — 当前名「荆棘」

- 神：磐垒（`bulwark`）　类别：防御　标签：defense, domain
- 身份契约：把承受转化为反击：反伤与突破点爆发
- 当前 overview：磐垒的出口：让撞墙者付出代价。

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.thorns.name` | 荆棘 |
| `cards.thorns.overview` | 磐垒的出口：让撞墙者付出代价。 |
| `cards.thorns.hand.shortByTier.1` | 核心载体 · 拖放释放 |
| `cards.thorns.hand.shortByTier.3` | 核心载体强化 · 拖放释放 |
| `cards.thorns.hand.shortByTier.6` | 公共终态 · 拖放释放 |
| `cards.thorns.hand.milestones.3` | **荆棘强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.thorns.hand.milestones.6` | **荆棘终态** / 解锁公共 6★ 终态。 （fx: transform） |
| `cards.thorns.equip.shortByTier.3` | 选择 荆棘/荆棘环/突破反刺 |
| `cards.thorns.equip.shortByTier.5` | 叠加兑现/传播/转化分支 |
| `cards.thorns.equip.shortByTier.6` | 公共终态 · 终态 |
| `cards.thorns.equip.milestones.3` | **荆棘强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.thorns.equip.milestones.5` | **荆棘再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.thorns.equip.milestones.6` | **荆棘终态** / 解锁公共 6★ 终态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；恢复最大生命的 4%
- **3★**：半径 140；生成半径 145 的光环，每 0.6 秒 结算；造成 0.35 倍爆发伤害
- **6★**：半径 160；生成半径 145 的光环，每 0.6 秒 结算；造成 0.35 倍爆发伤害；命中处引发范围爆发，半径 115，中心造成 130% 伤害，边缘衰减至 35%；处决生命低于 11% 的目标

### 3★ 分支（三选一）

#### `thornsA` — 当前分支名「荆棘」

精确效果：
- 持续生效 → 受到伤害时反弹 18% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.thorns.thornsA.name` | 荆棘 |
| `evolution.thorns.thornsA.summary` | 突破者受到按比例反伤（反伤致死按击杀结算，有奖励） |
| `evolution.thorns.thornsA.intent` | 荆棘以thorns @ passive承担核心机制。 |
| `evolution.thorns.thornsA.keywords` | thorns @ passive |
| `evolution.thorns.thornsA.buildFit` | 突破者受到按比例反伤（反伤致死按击杀结算，有奖励） |

#### `thornsB` — 当前分支名「荆棘环」

精确效果：
- 持续生效 → 受到伤害时反弹 20% 伤害；生成半径 95 的光环，每 0.35 秒 结算；　造成 0.13 倍爆发伤害，作用半径 0

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.thorns.thornsB.name` | 荆棘环 |
| `evolution.thorns.thornsB.summary` | 核心贴身荆棘环，持续伤害进入者 |
| `evolution.thorns.thornsB.intent` | 荆棘环以aura @ passive承担核心机制。 |
| `evolution.thorns.thornsB.keywords` | aura @ passive |
| `evolution.thorns.thornsB.buildFit` | 核心贴身荆棘环，持续伤害进入者 |

#### `thornsC` — 当前分支名「突破反刺」

精确效果：
- 敌人突破防线时 → 命中处引发范围爆发，半径 115，中心造成 130% 伤害，边缘衰减至 35%；造成 0.7 倍爆发伤害，作用半径 105
- 敌人突破防线时 → 受到伤害时反弹 34% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.thorns.thornsC.name` | 突破反刺 |
| `evolution.thorns.thornsC.summary` | 突破坐标爆发反刺 |
| `evolution.thorns.thornsC.intent` | 突破反刺以burstDamage @ onBreach（按 point）承担核心机制。 |
| `evolution.thorns.thornsC.keywords` | burstDamage @ onBreach（按 point） |
| `evolution.thorns.thornsC.buildFit` | 突破坐标爆发反刺 |

### 5★ 分支（三选一）

#### `thorns1x` — 当前分支名「荆棘爆发」　接口角色：payoff

精确效果：
- 敌人突破防线时（每 3 秒至多一次） → 造成 0.7 倍爆发伤害，作用半径 90

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.thorns.thorns1x.name` | 荆棘爆发 |
| `evolution.thorns.thorns1x.summary` | onBreach（按 point，cd 3s）→ burstDamage，倍率随当前 thorns 累计比例提升 |
| `evolution.thorns.thorns1x.intent` | 兑现接口：onBreach（按 point，cd 3s）→ burstDamage，倍率随当前 thorns 累计比例提升 |
| `evolution.thorns.thorns1x.keywords` | 兑现 |
| `evolution.thorns.thorns1x.buildFit` | onBreach（按 point，cd 3s）→ burstDamage，倍率随当前 thorns 累计比例提升 |

#### `thorns2x` — 当前分支名「荆棘蔓延」　接口角色：spread

精确效果：
- 持续生效 → 生成半径 90 的光环，每 0.6 秒 结算；　受到伤害时反弹 26% 伤害；召唤 1 个诱饵，生命 65，持续 6 秒

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.thorns.thorns2x.name` | 荆棘蔓延 |
| `evolution.thorns.thorns2x.summary` | passive → aura 扩张，且 thorns 对光环内敌人也生效 |
| `evolution.thorns.thorns2x.intent` | 传播接口：passive → aura 扩张，且 thorns 对光环内敌人也生效 |
| `evolution.thorns.thorns2x.keywords` | 传播 |
| `evolution.thorns.thorns2x.buildFit` | passive → aura 扩张，且 thorns 对光环内敌人也生效 |

#### `thorns3x` — 当前分支名「荆棘吸收」　接口角色：convert

精确效果：
- 敌人突破防线时（每 5 秒至多一次） → 恢复最大生命的 7%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.thorns.thorns3x.name` | 荆棘吸收 |
| `evolution.thorns.thorns3x.summary` | onBreach（按 point，cd 5s）→ restore（把承受转成回复） |
| `evolution.thorns.thorns3x.intent` | 转化接口：onBreach（按 point，cd 5s）→ restore（把承受转成回复） |
| `evolution.thorns.thorns3x.keywords` | 转化 |
| `evolution.thorns.thorns3x.buildFit` | onBreach（按 point，cd 5s）→ restore（把承受转成回复） |

### 4★ 公共节点

- 数值放大：{"chance":"+20"}

### 6★ 公共节点

- 持续生效 → 处决生命低于 11% 的目标

---

## `decoy` — 当前名「磐垒图腾」

- 神：磐垒（`bulwark`）　类别：控制　标签：control, defense
- 身份契约：召唤诱饵实体分摊压力、改写敌人仇恨
- 当前 overview：磐垒的控制：让敌人打错目标。（现存三个 3★ 同构，本卡全部重写）

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.decoy.name` | 磐垒图腾 |
| `cards.decoy.overview` | 磐垒的控制：让敌人打错目标。（现存三个 3★ 同构，本卡全部重写） |
| `cards.decoy.hand.shortByTier.1` | 核心载体 · 拖放释放 |
| `cards.decoy.hand.shortByTier.3` | 核心载体强化 · 拖放释放 |
| `cards.decoy.hand.shortByTier.6` | 公共终态 · 拖放释放 |
| `cards.decoy.hand.milestones.3` | **磐垒图腾强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.decoy.hand.milestones.6` | **磐垒图腾终态** / 解锁公共 6★ 终态。 （fx: transform） |
| `cards.decoy.equip.shortByTier.3` | 选择 诱饵/诱饵阵/爆裂诱饵 |
| `cards.decoy.equip.shortByTier.5` | 叠加兑现/传播/转化分支 |
| `cards.decoy.equip.shortByTier.6` | 公共终态 · 终态 |
| `cards.decoy.equip.milestones.3` | **磐垒图腾强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.decoy.equip.milestones.5` | **磐垒图腾再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.decoy.equip.milestones.6` | **磐垒图腾终态** / 解锁公共 6★ 终态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；召唤 1 个诱饵，生命 45，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒
- **3★**：半径 140；召唤 1 个诱饵，生命 45，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒
- **6★**：半径 160；召唤 1 个诱饵，生命 45，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒；命中处引发范围爆发，半径 55，中心造成 25% 伤害，边缘衰减至 50%；指定属性提高 116%，持续 5 秒，最多 4 层

### 3★ 分支（三选一）

#### `decoyA` — 当前分支名「诱饵」

精确效果：
- 每波开始时 → 召唤 1 个诱饵，生命 45，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒
- 命中敌人时 → 受到伤害时反弹 18% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.decoy.decoyA.name` | 诱饵 |
| `evolution.decoy.decoyA.summary` | 波开始召唤一具诱饵吸引攻击 |
| `evolution.decoy.decoyA.intent` | 诱饵以summon(kind='decoy') @ onWaveStart承担核心机制。 |
| `evolution.decoy.decoyA.keywords` | summon(kind='decoy') @ onWaveStart |
| `evolution.decoy.decoyA.buildFit` | 波开始召唤一具诱饵吸引攻击 |

#### `decoyB` — 当前分支名「诱饵阵」

精确效果：
- 每 9 秒 → 召唤 1 个诱饵，生命 65，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒
- 命中敌人时 → 受到伤害时反弹 26% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.decoy.decoyB.name` | 诱饵阵 |
| `evolution.decoy.decoyB.summary` | 周期补充诱饵，场上可同时存在多具 |
| `evolution.decoy.decoyB.intent` | 诱饵阵以summon(decoy) @ interval 9s承担核心机制。 |
| `evolution.decoy.decoyB.keywords` | summon(decoy) @ interval 9s |
| `evolution.decoy.decoyB.buildFit` | 周期补充诱饵，场上可同时存在多具 |

#### `decoyC` — 当前分支名「爆裂诱饵」

精确效果：
- 每波开始时 → 召唤 2 个诱饵，生命 85，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒，消失时爆炸并造成 0.4 倍伤害
- 命中敌人时 → 受到伤害时反弹 34% 伤害
- 命中敌人时 → 命中处引发范围爆发，半径 55，中心造成 25% 伤害，边缘衰减至 50%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.decoy.decoyC.name` | 爆裂诱饵 |
| `evolution.decoy.decoyC.summary` | 诱饵死亡时爆炸 |
| `evolution.decoy.decoyC.intent` | 爆裂诱饵以summon(decoy, explodeDamageMul>0) @ onWaveStart承担核心机制。 |
| `evolution.decoy.decoyC.keywords` | summon(decoy, explodeDamageMul>0) @ onWaveStart |
| `evolution.decoy.decoyC.buildFit` | 诱饵死亡时爆炸 |

### 5★ 分支（三选一）

#### `decoy1x` — 当前分支名「引爆」　接口角色：payoff

精确效果：
- 持续生效 → 召唤 1 个诱饵，生命 45，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.decoy.decoy1x.name` | 引爆 |
| `evolution.decoy.decoy1x.summary` | passive → summon.explodeDamageMul 大幅提升 |
| `evolution.decoy.decoy1x.intent` | 兑现接口：passive → summon.explodeDamageMul 大幅提升 |
| `evolution.decoy.decoy1x.keywords` | 兑现 |
| `evolution.decoy.decoy1x.buildFit` | passive → summon.explodeDamageMul 大幅提升 |

#### `decoy2x` — 当前分支名「图腾阵列」　接口角色：spread

精确效果：
- 每波开始时 → 生成半径 145 的光环，每 0.6 秒 结算；　受到伤害时反弹 26% 伤害；召唤 1 个诱饵，生命 70，持续 6 秒

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.decoy.decoy2x.name` | 图腾阵列 |
| `evolution.decoy.decoy2x.summary` | onWaveStart → 额外召唤 +1，且诱饵存在时 aura 减伤区以炮台为心展开 |
| `evolution.decoy.decoy2x.intent` | 传播接口：onWaveStart → 额外召唤 +1，且诱饵存在时 aura 减伤区以炮台为心展开 |
| `evolution.decoy.decoy2x.keywords` | 传播 |
| `evolution.decoy.decoy2x.buildFit` | onWaveStart → 额外召唤 +1，且诱饵存在时 aura 减伤区以炮台为心展开 |

#### `decoy3x` — 当前分支名「献祭」　接口角色：convert

精确效果：
- 每 5 秒 → 敌人突破造成的伤害降低 16%；恢复最大生命的 7%
- 持续生效 → 敌人突破造成的伤害降低 12%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.decoy.decoy3x.name` | 献祭 |
| `evolution.decoy.decoy3x.summary` | passive → 诱饵存在时 breachReduction + interval 5s restore |
| `evolution.decoy.decoy3x.intent` | 转化接口：passive → 诱饵存在时 breachReduction + interval 5s restore |
| `evolution.decoy.decoy3x.keywords` | 转化 |
| `evolution.decoy.decoy3x.buildFit` | passive → 诱饵存在时 breachReduction + interval 5s restore |

### 4★ 公共节点

- 数值放大：{"explodeDamageMul":"+0.3","explode":"+1"}

### 6★ 公共节点

- 持续生效 → 指定属性提高 116%，持续 5 秒，最多 4 层；受到伤害时反弹 34% 伤害

---

## `sanctum` — 当前名「圣域」

- 神：磐垒（`bulwark`）　类别：领域　标签：domain, control
- 身份契约：以炮台为心的减伤圣域（aura），用空间换生存
- 当前 overview：磐垒的领域表达。（现存 5★ 同构对，本卡重写）

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.sanctum.name` | 圣域 |
| `cards.sanctum.overview` | 磐垒的领域表达。（现存 5★ 同构对，本卡重写） |
| `cards.sanctum.hand.shortByTier.1` | 核心载体 · 拖放释放 |
| `cards.sanctum.hand.shortByTier.3` | 核心载体强化 · 拖放释放 |
| `cards.sanctum.hand.shortByTier.6` | 公共终态 · 拖放释放 |
| `cards.sanctum.hand.milestones.3` | **圣域强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.sanctum.hand.milestones.6` | **圣域终态** / 解锁公共 6★ 终态。 （fx: transform） |
| `cards.sanctum.equip.shortByTier.3` | 选择 圣域/扩张圣域/波末圣域 |
| `cards.sanctum.equip.shortByTier.5` | 叠加兑现/传播/转化分支 |
| `cards.sanctum.equip.shortByTier.6` | 公共终态 · 终态 |
| `cards.sanctum.equip.milestones.3` | **圣域强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.sanctum.equip.milestones.5` | **圣域再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.sanctum.equip.milestones.6` | **圣域终态** / 解锁公共 6★ 终态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；生成光环，半径为射程的 50%，每 0.6 秒 结算；造成 0.35 倍爆发伤害
- **3★**：半径 140；生成光环，半径为射程的 50%，每 0.6 秒 结算；造成 0.35 倍爆发伤害
- **6★**：半径 160；生成光环，半径为射程的 50%，每 0.6 秒 结算；造成 0.35 倍爆发伤害；伤害提高 108%，持续 3 秒，最多 2 层

### 3★ 分支（三选一）

#### `sanctumA` — 当前分支名「圣域」

精确效果：
- 持续生效 → 生成光环，半径为射程的 50%，每 0.6 秒 结算；　受到伤害时反弹 18% 伤害
- 命中敌人时 → 受到伤害时反弹 18% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.sanctum.sanctumA.name` | 圣域 |
| `evolution.sanctum.sanctumA.summary` | 核心周围常驻减伤圣域 |
| `evolution.sanctum.sanctumA.intent` | 圣域以aura @ passive承担核心机制。 |
| `evolution.sanctum.sanctumA.keywords` | aura @ passive |
| `evolution.sanctum.sanctumA.buildFit` | 核心周围常驻减伤圣域 |

#### `sanctumB` — 当前分支名「扩张圣域」

精确效果：
- 每 4 秒 → 生成半径 90 的光环，每 0.6 秒 结算；　受到伤害时反弹 26% 伤害
- 命中敌人时 → 受到伤害时反弹 26% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.sanctum.sanctumB.name` | 扩张圣域 |
| `evolution.sanctum.sanctumB.summary` | 周期向外推出扩张环，短时覆盖更远 |
| `evolution.sanctum.sanctumB.intent` | 扩张圣域以aura @ interval 4s承担核心机制。 |
| `evolution.sanctum.sanctumB.keywords` | aura @ interval 4s |
| `evolution.sanctum.sanctumB.buildFit` | 周期向外推出扩张环，短时覆盖更远 |

#### `sanctumC` — 当前分支名「波末圣域」

精确效果：
- 每波开始时 → 敌人突破造成的伤害降低 16%；生成半径 165 的光环，每 0.6 秒 结算；　受到伤害时反弹 34% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.sanctum.sanctumC.name` | 波末圣域 |
| `evolution.sanctum.sanctumC.summary` | 每波开始建立强化圣域，随波次推进衰减 |
| `evolution.sanctum.sanctumC.intent` | 波末圣域以aura+breachReduction @ onWaveStart承担核心机制。 |
| `evolution.sanctum.sanctumC.keywords` | aura+breachReduction @ onWaveStart |
| `evolution.sanctum.sanctumC.buildFit` | 每波开始建立强化圣域，随波次推进衰减 |

### 5★ 分支（三选一）

#### `sanctum1x` — 当前分支名「圣域反击」　接口角色：payoff

精确效果：
- 敌人突破防线时 → 造成 0.65 倍爆发伤害，作用半径 120

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.sanctum.sanctum1x.name` | 圣域反击 |
| `evolution.sanctum.sanctum1x.summary` | onBreach（按 point，cd 4s）→ burstDamage 范围，倍率随圣域减伤比提升 |
| `evolution.sanctum.sanctum1x.intent` | 兑现接口：onBreach（按 point，cd 4s）→ burstDamage 范围，倍率随圣域减伤比提升 |
| `evolution.sanctum.sanctum1x.keywords` | 兑现 |
| `evolution.sanctum.sanctum1x.buildFit` | onBreach（按 point，cd 4s）→ burstDamage 范围，倍率随圣域减伤比提升 |

#### `sanctum2x` — 当前分支名「圣域延展」　接口角色：spread

精确效果：
- 持续生效 → 生成半径 125 的光环，每 0.7 秒 结算；　敌人突破造成的伤害降低 14%
- 每波开始时 → 获得可抵挡 1 次伤害的护盾，每 10 秒 恢复

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.sanctum.sanctum2x.name` | 圣域延展 |
| `evolution.sanctum.sanctum2x.summary` | passive → aura 半径随场上敌人数增长（封顶） |
| `evolution.sanctum.sanctum2x.intent` | 传播接口：passive → aura 半径随场上敌人数增长（封顶） |
| `evolution.sanctum.sanctum2x.keywords` | 传播 |
| `evolution.sanctum.sanctum2x.buildFit` | passive → aura 半径随场上敌人数增长（封顶） |

#### `sanctum3x` — 当前分支名「圣域回复」　接口角色：convert

精确效果：
- 每 5 秒 → 恢复最大生命的 7%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.sanctum.sanctum3x.name` | 圣域回复 |
| `evolution.sanctum.sanctum3x.summary` | interval 5s → 该周期内无突破则 restore |
| `evolution.sanctum.sanctum3x.intent` | 转化接口：interval 5s → 该周期内无突破则 restore |
| `evolution.sanctum.sanctum3x.keywords` | 转化 |
| `evolution.sanctum.sanctum3x.buildFit` | interval 5s → 该周期内无突破则 restore |

### 4★ 公共节点

- 数值放大：{"radiusRatioOfRange":"+0.05"}

### 6★ 公共节点

- 持续生效 → 敌人突破造成的伤害降低 8%；伤害提高 108%，持续 3 秒，最多 2 层

---

## `sentinel` — 当前名「哨戒炮塔」

- 神：磐垒（`bulwark`）　类别：弹道　标签：projectile, defense
- 身份契约：召唤协同射击的哨戒单位，用编队火力换防线厚度
- 当前 overview：磐垒的弹道表达：不是炮更强，是炮更多。（现存三个 3★ 同构，本卡全部重写）

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.sentinel.name` | 哨戒炮塔 |
| `cards.sentinel.overview` | 磐垒的弹道表达：不是炮更强，是炮更多。（现存三个 3★ 同构，本卡全部重写） |
| `cards.sentinel.hand.shortByTier.1` | 核心载体 · 拖放释放 |
| `cards.sentinel.hand.shortByTier.3` | 核心载体强化 · 拖放释放 |
| `cards.sentinel.hand.shortByTier.6` | 公共终态 · 拖放释放 |
| `cards.sentinel.hand.milestones.3` | **哨戒炮塔强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.sentinel.hand.milestones.6` | **哨戒炮塔终态** / 解锁公共 6★ 终态。 （fx: transform） |
| `cards.sentinel.equip.shortByTier.3` | 选择 哨戒/轨道哨戒/应急哨戒 |
| `cards.sentinel.equip.shortByTier.5` | 叠加兑现/传播/转化分支 |
| `cards.sentinel.equip.shortByTier.6` | 公共终态 · 终态 |
| `cards.sentinel.equip.milestones.3` | **哨戒炮塔强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.sentinel.equip.milestones.5` | **哨戒炮塔再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.sentinel.equip.milestones.6` | **哨戒炮塔终态** / 解锁公共 6★ 终态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；召唤 1 个单位，生命 45，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒
- **3★**：半径 140；召唤 1 个单位，生命 45，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒；弹道穿透 1 个目标，每次保留 90% 伤害
- **6★**：半径 160；召唤 1 个单位，生命 45，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒；弹道穿透 1 个目标，每次保留 90% 伤害；攻速提高 112%，持续 4 秒，最多 3 层

### 3★ 分支（三选一）

#### `sentinelA` — 当前分支名「哨戒」

精确效果：
- 每波开始时 → 召唤 1 个单位，生命 45，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒
- 命中敌人时 → 受到伤害时反弹 18% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.sentinel.sentinelA.name` | 哨戒 |
| `evolution.sentinel.sentinelA.summary` | 召唤镜像炮塔协同射击 |
| `evolution.sentinel.sentinelA.intent` | 哨戒以summon(kind='mirrorTurret') @ onWaveStart承担核心机制。 |
| `evolution.sentinel.sentinelA.keywords` | summon(kind='mirrorTurret') @ onWaveStart |
| `evolution.sentinel.sentinelA.buildFit` | 召唤镜像炮塔协同射击 |

#### `sentinelB` — 当前分支名「轨道哨戒」

精确效果：
- 每波开始时 → 召唤 1 个单位，生命 65，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒
- 命中敌人时 → 受到伤害时反弹 26% 伤害
- 每次开火时 → 弹道穿透 1 个目标，每次保留 90% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.sentinel.sentinelB.name` | 轨道哨戒 |
| `evolution.sentinel.sentinelB.summary` | 召唤环绕轨道单位，覆盖面更广 |
| `evolution.sentinel.sentinelB.intent` | 轨道哨戒以summon(kind='orbital') @ onWaveStart承担核心机制。 |
| `evolution.sentinel.sentinelB.keywords` | summon(kind='orbital') @ onWaveStart |
| `evolution.sentinel.sentinelB.buildFit` | 召唤环绕轨道单位，覆盖面更广 |

#### `sentinelC` — 当前分支名「应急哨戒」

精确效果：
- 敌人突破防线时 → 召唤 2 个单位，生命 85，持续 8 秒，攻击造成 35% 伤害，攻击间隔 0.8 秒
- 敌人突破防线时 → 受到伤害时反弹 34% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.sentinel.sentinelC.name` | 应急哨戒 |
| `evolution.sentinel.sentinelC.summary` | 突破坐标紧急部署哨戒 |
| `evolution.sentinel.sentinelC.intent` | 应急哨戒以summon(mirrorTurret) @ onBreach（按 point）承担核心机制。 |
| `evolution.sentinel.sentinelC.keywords` | summon(mirrorTurret) @ onBreach（按 point） |
| `evolution.sentinel.sentinelC.buildFit` | 突破坐标紧急部署哨戒 |

### 5★ 分支（三选一）

#### `sentinel1x` — 当前分支名「哨戒火力」　接口角色：payoff

精确效果：
- 持续生效 → 造成 1.05 倍爆发伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.sentinel.sentinel1x.name` | 哨戒火力 |
| `evolution.sentinel.sentinel1x.summary` | passive → 哨戒伤害提升；壁垒存在时额外再提升 |
| `evolution.sentinel.sentinel1x.intent` | 兑现接口：passive → 哨戒伤害提升；壁垒存在时额外再提升 |
| `evolution.sentinel.sentinel1x.keywords` | 兑现 |
| `evolution.sentinel.sentinel1x.buildFit` | passive → 哨戒伤害提升；壁垒存在时额外再提升 |

#### `sentinel2x` — 当前分支名「哨戒阵」　接口角色：spread

精确效果：
- 每波开始时 → 召唤 1 个单位，生命 70，持续 8 秒，攻击造成 40% 伤害，攻击间隔 0.8 秒；受到伤害时反弹 26% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.sentinel.sentinel2x.name` | 哨戒阵 |
| `evolution.sentinel.sentinel2x.summary` | onWaveStart → 哨戒数量 +1 |
| `evolution.sentinel.sentinel2x.intent` | 传播接口：onWaveStart → 哨戒数量 +1 |
| `evolution.sentinel.sentinel2x.keywords` | 传播 |
| `evolution.sentinel.sentinel2x.buildFit` | onWaveStart → 哨戒数量 +1 |

#### `sentinel3x` — 当前分支名「哨戒护盾」　接口角色：convert

精确效果：
- 持续生效 → 敌人突破造成的伤害降低 16%

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.sentinel.sentinel3x.name` | 哨戒护盾 |
| `evolution.sentinel.sentinel3x.summary` | passive → 每有一具哨戒存活，提供一档 breachReduction |
| `evolution.sentinel.sentinel3x.intent` | 转化接口：passive → 每有一具哨戒存活，提供一档 breachReduction |
| `evolution.sentinel.sentinel3x.keywords` | 转化 |
| `evolution.sentinel.sentinel3x.buildFit` | passive → 每有一具哨戒存活，提供一档 breachReduction |

### 4★ 公共节点

- 数值放大：{"damageRetention":"+0.1"}

### 6★ 公共节点

- 持续生效 → 攻速提高 112%，持续 4 秒，最多 3 层；受到伤害时反弹 26% 伤害

---

## `retribution` — 当前名「天罚」

- 神：磐垒（`bulwark`）　类别：控制　标签：control, defense
- 身份契约：突破事件本身是弹药：每次突破在突破点触发审判打击
- 当前 overview：磐垒的控制：被突破的每一寸都还回去。（现存三个 3★ 同构 + onBreach 打空 ×18，全表最重，全部重写为按 point）

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.retribution.name` | 天罚 |
| `cards.retribution.overview` | 磐垒的控制：被突破的每一寸都还回去。（现存三个 3★ 同构 + onBreach 打空 ×18，全表最重，全部重写为按 point） |
| `cards.retribution.hand.shortByTier.1` | 核心载体 · 拖放释放 |
| `cards.retribution.hand.shortByTier.3` | 核心载体强化 · 拖放释放 |
| `cards.retribution.hand.shortByTier.6` | 公共终态 · 拖放释放 |
| `cards.retribution.hand.milestones.3` | **天罚强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.retribution.hand.milestones.6` | **天罚终态** / 解锁公共 6★ 终态。 （fx: transform） |
| `cards.retribution.equip.shortByTier.3` | 选择 天罚/震慑/蓄能天罚 |
| `cards.retribution.equip.shortByTier.5` | 叠加兑现/传播/转化分支 |
| `cards.retribution.equip.shortByTier.6` | 公共终态 · 终态 |
| `cards.retribution.equip.milestones.3` | **天罚强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.retribution.equip.milestones.5` | **天罚再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.retribution.equip.milestones.6` | **天罚终态** / 解锁公共 6★ 终态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；命中处引发范围爆发，半径 85，中心造成 80% 伤害，边缘衰减至 35%
- **3★**：半径 140；命中处引发范围爆发，半径 85，中心造成 80% 伤害，边缘衰减至 35%；弹道穿透 1 个目标，每次保留 90% 伤害
- **6★**：半径 160；命中处引发范围爆发，半径 85，中心造成 80% 伤害，边缘衰减至 35%；弹道穿透 1 个目标，每次保留 90% 伤害

### 3★ 分支（三选一）

#### `retributionA` — 当前分支名「天罚」

精确效果：
- 敌人突破防线时 → 造成 0.9 倍爆发伤害，作用半径 90

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.retribution.retributionA.name` | 天罚 |
| `evolution.retribution.retributionA.summary` | 突破坐标降下天罚 |
| `evolution.retribution.retributionA.intent` | 天罚以burstDamage @ onBreach（按 point）承担核心机制。 |
| `evolution.retribution.retributionA.keywords` | burstDamage @ onBreach（按 point） |
| `evolution.retribution.retributionA.buildFit` | 突破坐标降下天罚 |

#### `retributionB` — 当前分支名「震慑」

精确效果：
- 敌人突破防线时 → 命中处引发范围爆发，半径 130，中心造成 70% 伤害，边缘衰减至 35%；击退目标 70 距离，碰撞造成 15% 伤害，作用半径 130

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.retribution.retributionB.name` | 震慑 |
| `evolution.retribution.retributionB.summary` | 大范围震慑并击退 |
| `evolution.retribution.retributionB.intent` | 震慑以aoeOnHit+knockback @ onBreach（按 point）承担核心机制。 |
| `evolution.retribution.retributionB.keywords` | aoeOnHit+knockback @ onBreach（按 point） |
| `evolution.retribution.retributionB.buildFit` | 大范围震慑并击退 |

#### `retributionC` — 当前分支名「蓄能天罚」

精确效果：
- 敌人突破防线时 → 造成 0.75 倍爆发伤害，作用半径 105

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.retribution.retributionC.name` | 蓄能天罚 |
| `evolution.retribution.retributionC.summary` | 倍率随「上次突破以来的时长」累积，越久未破越强 |
| `evolution.retribution.retributionC.intent` | 蓄能天罚以burstDamage @ onBreach（按 point）承担核心机制。 |
| `evolution.retribution.retributionC.keywords` | burstDamage @ onBreach（按 point） |
| `evolution.retribution.retributionC.buildFit` | 倍率随「上次突破以来的时长」累积，越久未破越强 |

### 5★ 分支（三选一）

#### `retribution1x` — 当前分支名「审判」　接口角色：payoff

精确效果：
- 敌人突破防线时 → 造成 0.8 倍爆发伤害，作用半径 105

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.retribution.retribution1x.name` | 审判 |
| `evolution.retribution.retribution1x.summary` | onBreach（按 point）→ burstDamage，倍率随壁垒剩余容量提升 |
| `evolution.retribution.retribution1x.intent` | 兑现接口：onBreach（按 point）→ burstDamage，倍率随壁垒剩余容量提升 |
| `evolution.retribution.retribution1x.keywords` | 兑现 |
| `evolution.retribution.retribution1x.buildFit` | onBreach（按 point）→ burstDamage，倍率随壁垒剩余容量提升 |

#### `retribution2x` — 当前分支名「连环天罚」　接口角色：spread

精确效果：
- 敌人突破防线时（每 5 秒至多一次） → 生成半径 150 的光环，每 0.6 秒 结算；　伤害提高 112%，持续 1 秒，最多 2 层；召唤 1 个诱饵，生命 65，持续 5 秒

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.retribution.retribution2x.name` | 连环天罚 |
| `evolution.retribution.retribution2x.summary` | onBreach（按 point，cd 5s）→ aura 短时全场增伤 |
| `evolution.retribution.retribution2x.intent` | 传播接口：onBreach（按 point，cd 5s）→ aura 短时全场增伤 |
| `evolution.retribution.retribution2x.keywords` | 传播 |
| `evolution.retribution.retribution2x.buildFit` | onBreach（按 point，cd 5s）→ aura 短时全场增伤 |

#### `retribution3x` — 当前分支名「罪罚转化」　接口角色：convert

精确效果：
- 敌人突破防线时（每 6 秒至多一次） → 恢复最大生命的 7%；伤害提高 116%，持续 5 秒，最多 4 层

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.retribution.retribution3x.name` | 罪罚转化 |
| `evolution.retribution.retribution3x.summary` | onBreach（按 point，cd 6s）→ restore + statBuff 伤害 |
| `evolution.retribution.retribution3x.intent` | 转化接口：onBreach（按 point，cd 6s）→ restore + statBuff 伤害 |
| `evolution.retribution.retribution3x.keywords` | 转化 |
| `evolution.retribution.retribution3x.buildFit` | onBreach（按 point，cd 6s）→ restore + statBuff 伤害 |

### 4★ 公共节点

- 数值放大：{"damageRatio":"+1"}

### 6★ 公共节点

- 持续生效 → 敌人突破造成的伤害降低 16%；受到伤害时反弹 34% 伤害

---

## `ironvine` — 当前名「铁蔓」

- 神：磐垒（`bulwark`）　类别：经济　标签：utility, defense
- 身份契约：守成经济：无突破的时间与投入返还转化为产出
- 当前 overview：磐垒的经济：守得住，就有收成。（修复 requiresSource:'retaliation' 不可达分支）

### 当前文案槽位

| JSON 路径 | 当前值 |
|---|---|
| `cards.ironvine.name` | 铁蔓 |
| `cards.ironvine.overview` | 磐垒的经济：守得住，就有收成。（修复 requiresSource:'retaliation' 不可达分支） |
| `cards.ironvine.hand.shortByTier.1` | 核心载体 · 拖放释放 |
| `cards.ironvine.hand.shortByTier.3` | 核心载体强化 · 拖放释放 |
| `cards.ironvine.hand.shortByTier.6` | 公共终态 · 拖放释放 |
| `cards.ironvine.hand.milestones.3` | **铁蔓强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.ironvine.hand.milestones.6` | **铁蔓终态** / 解锁公共 6★ 终态。 （fx: transform） |
| `cards.ironvine.equip.shortByTier.3` | 选择 铁蔓/蔓生领域/守成结算 |
| `cards.ironvine.equip.shortByTier.5` | 叠加兑现/传播/转化分支 |
| `cards.ironvine.equip.shortByTier.6` | 公共终态 · 终态 |
| `cards.ironvine.equip.milestones.3` | **铁蔓强化** / 选择一条 3★ 进化分支。 （fx: core） |
| `cards.ironvine.equip.milestones.5` | **铁蔓再进化** / 独立叠加一条 5★ 接口分支。 （fx: major） |
| `cards.ironvine.equip.milestones.6` | **铁蔓终态** / 解锁公共 6★ 终态。 （fx: transform） |

### 消耗态精确效果（拖到战场释放）

- **1★**：半径 120；伤害提高 108%，持续 3 秒，最多 2 层；恢复最大生命的 4%
- **3★**：半径 140；伤害提高 108%，持续 3 秒，最多 2 层；生成半径 145 的光环，每 0.6 秒 结算；造成 0.35 倍爆发伤害
- **6★**：半径 160；伤害提高 108%，持续 3 秒，最多 2 层；生成半径 145 的光环，每 0.6 秒 结算；造成 0.35 倍爆发伤害

### 3★ 分支（三选一）

#### `ironvineA` — 当前分支名「铁蔓」

精确效果：
- 持续生效 → 伤害提高 108%，持续 3 秒，最多 2 层；受到伤害时反弹 18% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.ironvine.ironvineA.name` | 铁蔓 |
| `evolution.ironvine.ironvineA.summary` | 反伤；且「距上次突破的时长」越久，伤害加成越高（封顶） |
| `evolution.ironvine.ironvineA.intent` | 铁蔓以thorns+statBuff @ passive承担核心机制。 |
| `evolution.ironvine.ironvineA.keywords` | thorns+statBuff @ passive |
| `evolution.ironvine.ironvineA.buildFit` | 反伤；且「距上次突破的时长」越久，伤害加成越高（封顶） |

#### `ironvineB` — 当前分支名「蔓生领域」

精确效果：
- 持续生效 → 受到伤害时反弹 12% 伤害；生成半径 170 的光环，每 0.8 秒 结算；　造成 0.04 倍爆发伤害，作用半径 0

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.ironvine.ironvineB.name` | 蔓生领域 |
| `evolution.ironvine.ironvineB.summary` | 常驻铁蔓领域，范围内敌人持续受伤 |
| `evolution.ironvine.ironvineB.intent` | 蔓生领域以aura @ passive承担核心机制。 |
| `evolution.ironvine.ironvineB.keywords` | aura @ passive |
| `evolution.ironvine.ironvineB.buildFit` | 常驻铁蔓领域，范围内敌人持续受伤 |

#### `ironvineC` — 当前分支名「守成结算」

精确效果：
- 每波开始时 → 指定属性提高 116%，持续 5 秒，最多 4 层
- 命中敌人时 → 受到伤害时反弹 34% 伤害

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.ironvine.ironvineC.name` | 守成结算 |
| `evolution.ironvine.ironvineC.summary` | 上一波无突破则获得整波强化 |
| `evolution.ironvine.ironvineC.intent` | 守成结算以statBuff @ onWaveStart承担核心机制。 |
| `evolution.ironvine.ironvineC.keywords` | statBuff @ onWaveStart |
| `evolution.ironvine.ironvineC.buildFit` | 上一波无突破则获得整波强化 |

### 5★ 分支（三选一）

#### `ironvine1x` — 当前分支名「荆棘收割」　接口角色：payoff

精确效果：
- 敌人突破防线时（每 3 秒至多一次） → 造成 0.65 倍爆发伤害，作用半径 105

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.ironvine.ironvine1x.name` | 荆棘收割 |
| `evolution.ironvine.ironvine1x.summary` | onBreach（按 point，cd 3s）→ burstDamage，倍率随累计 thorns 提升 |
| `evolution.ironvine.ironvine1x.intent` | 兑现接口：onBreach（按 point，cd 3s）→ burstDamage，倍率随累计 thorns 提升 |
| `evolution.ironvine.ironvine1x.keywords` | 兑现 |
| `evolution.ironvine.ironvine1x.buildFit` | onBreach（按 point，cd 3s）→ burstDamage，倍率随累计 thorns 提升 |

#### `ironvine2x` — 当前分支名「蔓延」　接口角色：spread

精确效果：
- 持续生效 → 受到伤害时反弹 32% 伤害；生成半径 120 的光环，每 0.7 秒 结算；　受到伤害时反弹 12% 伤害；召唤 1 个诱饵，生命 70，持续 6 秒

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.ironvine.ironvine2x.name` | 蔓延 |
| `evolution.ironvine.ironvine2x.summary` | passive → aura 扩张 + thorns 追加一档 |
| `evolution.ironvine.ironvine2x.intent` | 传播接口：passive → aura 扩张 + thorns 追加一档 |
| `evolution.ironvine.ironvine2x.keywords` | 传播 |
| `evolution.ironvine.ironvine2x.buildFit` | passive → aura 扩张 + thorns 追加一档 |

#### `ironvine3x` — 当前分支名「保护投入」　接口角色：convert

精确效果：
- 持续生效 → 普通合并或装备喂养时，有 20% 概率返还 1 张 1★ 同型素材卡；45% 概率额外生成 1 个掉落物

当前文案（问题样本）：

| 路径 | 当前值 |
|---|---|
| `evolution.ironvine.ironvine3x.name` | 保护投入 |
| `evolution.ironvine.ironvine3x.summary` | passive → mergeMaterialRefund（磐垒例外持有的经济规则原子，全表首次启用）+ 无突破波次结束时 extraDrop |
| `evolution.ironvine.ironvine3x.intent` | 转化接口：passive → mergeMaterialRefund（磐垒例外持有的经济规则原子，全表首次启用）+ 无突破波次结束时 extraDrop |
| `evolution.ironvine.ironvine3x.keywords` | 转化 |
| `evolution.ironvine.ironvine3x.buildFit` | passive → mergeMaterialRefund（磐垒例外持有的经济规则原子，全表首次启用）+ 无突破波次结束时 extraDrop |

### 4★ 公共节点

- 数值放大：{"value":"+0.1","maxStacks":"+1"}

### 6★ 公共节点

- 每波开始时 → 敌人突破造成的伤害降低 8%；伤害提高 108%，持续 3 秒，最多 2 层
