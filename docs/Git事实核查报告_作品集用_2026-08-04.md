# ProjectVL 事实核查报告——为战斗策划作品集提供可信证据

生成时间：2026-08-04
性质：对你提供的 ChatGPT 版本报告逐条核查后的**事实分析**，不是又一份平铺的 commit 流水账。核查手段：本地 `git log`/`git show`、GitHub API 真实 PR/Issue 数据（`SageLys/ProjectVL` 是真实公开仓库）、本地 `docs/`、`src/config/base/*.json` 源文件、实际跑通 `npm run validate`、`npx vitest`、`npx vite-node scripts/validateTextsFile.ts`。

结论先说：ChatGPT 报告里**最有价值的那条主线——"约27300局仿真全绿、真人玩90秒判定无聊"——是真的，而且比它转述得更精确、更适合直接当作品集第一章**。但报告里一半的"仓库状态"类事实是错的或过时的（PR数量、测试数量归属、"遥测文件为空"），这类错误如果直接抄进作品集，见到仓库的面试官一眼能戳穿。以下逐条给出核查结果。

---

## 一、逐条核查：哪些是真的，哪些错了

| ChatGPT 的主张 | 核查结论 | 证据 |
|---|---|---|
| 27300局仿真全绿、真人90秒判定无聊、五个根因、E1-E7指标 | **完全属实，且原文精度更高** | `docs/P6_体验重标定_反思与重构计划.md`（2026-07-12）：P4跑9000局+P4.1跑18000局+P5复跑300局=27300局；波1精确复盘表（TTK≈0.85s、DPS 52.8、45HP、出怪间隔5.12s、射程430）；"Bot验证一致性，人验证乐趣"原文原句 |
| 装备从"永久不可逆+纯销毁"改为"可消耗释放" | **属实，且有明确拍板日期** | `docs/可玩原型_重启开发总计划.md`：2026-07-14 拍板，`equipIrreversible=false`、`unequipPolicy="consume"`；对应commit `3f31f5a`（07-14 "allow equipped cards to be consumed"）；当前 `src/config/base/economy.json` 仍是此值 |
| 主炮融合 impactShare=0.41416083945517623，1.35×混装收益，81文件/686测试 | **完全属实**，但**归属PR搞错了** | 这是 **PR #5**（"Fix weapon form fusion axes and growth budgets"，2026-07-28合并）的真实PR描述，逐字符匹配。ChatGPT把它错记成了"PR #5时的通用测试规模"，但没意识到这就是第4章要讲的同一个PR——本质是同一件事，只是它自己前后没对上 |
| 主动回退到P3完成态，废弃P4/P4.1/P5结论，重启开发流程 | **属实** | `docs/可玩原型_重启开发总计划.md`第5行原文："仓库已主动回退到P4开始前（=P3完成态：六域配置+effects解释器+108测试绿+5张占位卡）。P4/P4.1/P5的代码与数值结论全部废弃" |
| "两条新的验收原则"（界面必须真换掉/真人验收是唯一出口） | **属实，且是原文标题"两条铁律"** | 同上文档 §0，原文用词就是"铁律一/铁律二"，与ChatGPT转述内容一致 |
| 仓库有4个PR，3个已合并、1个仍是早期草稿 | **错，实际是5个PR** | GitHub API：#1开放草稿（P4/P4.1/P5内容，191测试）、#2已合并（装备流程，109测试）、#3已合并（技能schemav0.4.0，119测试）、#4**开放草稿**（缺失未提及）、#5已合并（主炮融合，686测试）。ChatGPT完全漏掉了#4 |
| 0个独立Issue | **属实** | GitHub API：`issues`端点返回的5条全部带`pull_request`字段，没有一条是纯Issue |
| 当前main HEAD的两个遥测文件是"空内容" | **错，是重要错误** | 实际用Python解析：两个文件分别有4448条events+3037条samples+187条inputs（另一个规模类似），文件大小1.1MB/2.0MB，绝非空文件。这个错误如果被面试官当场打开GitHub验证，比其他所有错误更伤可信度 |
| README仍写"11张技能卡、108项测试"，已过时 | **属实** | 当前`README.md`第16行原文确有"11 张技能卡"；当前`src/config/base/skills.json`实际有**60张卡**（5神×12张）、`docs/skills-schema.json`记录测试数已到108（早期），当前`tests/`目录实测**94个测试文件** |
| PR#1时19个测试文件/191测试，PR#5时81个测试文件/686测试 | **属实** | GitHub API PR body原文数字，逐字匹配 |
| 无法核实当前main精确测试用例数 | **部分属实** | 完整跑`npx vitest run`因单次shell调用有45秒上限、94个文件跑不完整未能拿到官方汇总行；抽样跑了约40%的测试文件（含`effectInterpreter`/`atomContract`/`bossBehavior`/`configLoader`等核心系统）全部通过，无失败；用`grep`静态计数`it(`/`test(`得到约601处，可作为下限估计，不是精确值 |

---

## 二、ChatGPT完全没提到、但对作品集更重要的三个新发现

### 1. 配置管线和文本管线现在真的是绿的，不是空谈
实际执行 `npx vite-node scripts/validateConfig.ts`：base与dev-short两个variant，16项检查（schema/跨引用/语义三层）全部通过，0错误0警告。`npx vite-node scripts/validateTextsFile.ts`：170699字节、20个顶层key、35个进化节点、210条分支，全部校验通过。这两个脚本本身就是"main编译不过与恢复记录"事故后新增的防复发机制（见你的 memory `projectvl-main-broken-2afc292-recovery`），现在处于可验证的绿色状态——这是"工程可维护性"的直接证据，比任何文字描述都硬。

### 2. 项目积累了46份真实遥测会话，不是2份空文件
`telemetry/` 目录下有**46个**真实会话JSON，横跨2026-07-13到07-31（其中07-14单日15个，对应装备语义调试密集期）。每个会话记录 `events`（技能选择、Boss出现等结构化事件）、`samples`（每秒同屏敌人数采样）、`inputs`（玩家实际操作，含 `bountyAccept` 这类语义化输入）。这意味着"用数据驱动调参"不是一句空话，而是全程留了痕——这比重新去"采集真人遥测"有价值得多，因为数据已经在仓库里，只是需要你自己写脚本聚合一下（比如按会话画出"前90秒操作数"随开发进度变化的曲线，直接可视化"90秒无聊→变好玩"这条主线的量化证据）。

### 3. 有一个真实协作者在做团结引擎（Unity中国）移植，而且已经相当完整
PR #4（开放中，草稿状态，2026-07-28创建，2026-08-03最后更新）作者是 `wlh219627-art`——不是你，是仓库的一名协作者（`author_association: COLLABORATOR`）。该PR基于 `agent/unity-port` 分支，**120次提交、414个文件改动、+65774/-4行**，PR描述里写的验证结果是"团结EditMode测试240/240通过"、"卡池核对35/35张普通卡+6/6张配方终态卡缺失为0"、"Windows构建成功"。

这条线索的价值：如果作品集想体现"设计决策具备跨引擎迁移到生产环境的可执行性"，这是最直接的证据——已经有第三方工程师能照着你的配置契约（`src/config/base/*.json`）在完全不同的引擎里独立复刻出可玩版本，这本身证明了你的数值/规则抽象是可迁移的，而不是绑死在这套TS实现里的。这一点比自己写"系统设计能力强"要有说服力得多。

---

## 三、按证据强度分级后，真正能写进作品集的故事

不是所有能查到的东西都值得写。以下按"证据链完整度"排序，标注每条故事目前手头有什么、还缺什么。

### 强证据（可以直接写，且经得起面试官追问）

**故事A：27300局仿真全绿 vs 真人90秒判定无聊**
证据：`docs/P6_体验重标定_反思与重构计划.md`全文、对应commit时间段（07-10 e72988f 重构配置 → 07-12 三条docs commit → 07-14起R1手动调参环相关commit）、`docs/P6_新KPI体系_v2_定版.md`（E1-E7定版）、`docs/R0_体验目标修订_v2草案.md`（注意力预算两层化）。
还缺：一张"改造前后"的可视化对比——可以用46份遥测里07-13前后的几个会话，脚本跑出"同屏敌人数"曲线对比图，比纯文字有冲击力。

**故事B：装备语义从"永久不可逆"到"可消耗释放"的推翻重来**
证据：PR#2原始设计（07-13合并）→ commit `3f31f5a`（07-14推翻）→ `docs/可玩原型_重启开发总计划.md`里明确写着拍板日期和理由。
还缺：推翻的**具体触发原因**目前文档只写了结果没写"为什么"（是试玩发现的问题，还是设计推演发现的问题）——如果这段对话记录还在，值得补一句设计动机，否则这个故事只有"改了"没有"为什么改"。

**故事C：主炮形态融合的正交轴设计与预算标定（PR#5）**
证据：PR#5完整body（delivery覆盖轴/impact叠加轴/baselineDps预算/impactShare反推过程/三档成长一致性表格），这是**唯一一条有完整"问题-模型-标定过程-验证结果"闭环、且已经合并进main的故事**，含精确到小数点后17位的反推值和6行验证表格，任何面试官都能在GitHub上直接翻到这个PR核实。
不缺什么——这是四条里证据最完整的一条，建议作为作品集"数值设计能力"的主展品。

### 中等证据（能写，但需要你自己补充说明，不要照抄ChatGPT的润色）

**故事D：从占位卡到60张正式卡+25配方+5神体系的内容规模化**
现状核实：当前`skills.json`是**60张卡**（每神12张，5神×12=60），`evolutionRecipes.json`**25个配方**——不是ChatGPT说的"35卡+6配方=41"，也不是你memory里记的"35+6"，这两个数字对应的都是7月30日左右的中间状态，之后内容还在继续扩到60张。写作品集时用**当前仓库的真实数字**，并说明这是"持续内容生产能力"而非一次性产出，比强调某个历史快照的数字更真实。

### 证据不足、目前不建议写（除非你能补材料）

- **具体的"before/after"游玩截图或录屏**：仓库里`artifacts/`目录只有约10张零散截图（`ui-layout-rework.png`等），不构成完整的版本对比证据链，需要你自己在几个关键commit上重新跑一遍构建、截图。
- **"27300局仿真"的原始仿真报告文件**：文档里引用了结论数字，但我没能在当前工作区定位到P4/P4.1原始仿真输出文件本体（可能在`legacy/`或已被后续commit清理）——如果要在作品集里展示仿真报告截图，需要你确认这些文件是否还在某个历史commit或分支里。

---

## 四、写作品集前，建议你亲自确认的三件事

1. **PR#1（191测试、19文件）目前仍是打开状态的草稿，从未合并。** 如果作品集要引用它，说清楚"这是被真人测试推翻的基线快照，特意保留未合并作为对照"，不要让人误以为这是当前状态。
2. **PR#4是协作者`wlh219627-art`的工作，不是你独立完成的。** 如果要在作品集里提团结引擎移植，措辞上要区分清楚"我设计的规则被协作者独立复刻验证"和"我做的移植"——这两者对战斗策划岗位的评价维度不同，前者甚至更有说服力（证明设计文档足够清晰，别人能看懂并照做）。
3. **README当前信息已过时（11卡/108测试 vs 实际60卡/94文件）。** 如果面试官会点开仓库，建议先花10分钟更新README的数字，或者在作品集里明确注明"README为早期工程脚手架说明，内容规模以下方数据为准"，避免自证矛盾。

---

需要的话，我可以下一步做：把故事C（主炮融合）单独整理成一页带真实PR截图/表格的案例页；或者写个小脚本聚合46份遥测数据，画出"同屏敌人数/操作机会"随开发时间的变化曲线，把故事A的"90秒无聊"用真实数据可视化出来。
