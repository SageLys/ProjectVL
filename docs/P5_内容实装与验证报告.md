# P5 内容实装与验证报告

日期：2026-07-12

## 已实装

- 12 张正式技能卡全部进入 `skills.json`：每张包含 2★/3★装备态与 1★/2★/3★落点消耗态。
- 每局从 12 张目录随机固定 5 张活跃池；万能卡不进入普通池。
- 同调 3★ 的 `wildcardDrop` 已接到 Bounty/Boss 击杀，万能卡可与任意同星卡合成并继承非万能卡类型。
- 诱饵图腾、摧毁爆炸、镜像炮台均走通用 summon 运行时。
- perk 池由 3 项扩为 9 项，每次仍只轮换展示 3 项。
- 浏览器 DEV API 新增 `window.__game.getRunStats()`；headless JSON 每局新增 `activeCardPool` 与 `finalBuild`。

## 自动验收

- `tests/p5FormalContent.test.ts`：12 张目录结构 + 每张两种用法 + 万能卡，共 27 项。
- 全仓测试：191 项。
- 生产构建通过。

## P4.1 接入后复跑

证据：`outputs/p5_20260712/P5_attention_base_retuned_100.json`（标准难度 × fast/target/stressed × 100，共 300 局）。这是正式 core 的 bot 数据，不冒充真人 T2/T3。

Target 初步结果：通关 52%、墙钟 P50 12.72 分钟、有效拾取 P50 35、合成 2.07/普通波、过期 8.15%、3s/10s P95 1.25/3、队列 P95 0.95 秒、消耗 0.70/分钟、Bounty 接受 53.6%/完成 71.2%。

与旧 P4.1 Target（18.9% 通关、43 拾取、2.36 合成/波、Boss 117.65 秒）相比，正式内容使构筑分布和 Boss 控制行为发生明显漂移：通关率过高，胜局 Boss P50 约 434 秒，不能沿用旧数值结论。点击压力仍在原工程包络内，但战斗标定未通过。

## 尚未完成的实证门

- T2 必须由施测者实际自玩并用 `getRunStats()` + `getAttentionTelemetry()` 导出；仓库内自动 bot 数据不能替代真人数据。
- T3 需要至少 5 名目标画像玩家，当前环境无法代替招募与观察。
- 因正式内容初跑未命中 P1 战斗 KPI，下一轮应先按“活跃池/构筑”分层检查胜率和 Boss 控制链，再调 Boss 的控制抗性或截止线；不建议只继续提高基础移速。
