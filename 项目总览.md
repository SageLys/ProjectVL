# ProjectVL 项目总览

更新时间：2026-07-12

## 项目定位

「魅魔心防战 / 炮台射击」360° 塔防可玩原型。已从单文件 HTML 原型（V0.2）重构为
**模块化 Vite + TypeScript 工程**：玩法规则可测试、数值可配置、模块边界清晰，
便于后续 AI Agent 安全地做局部迭代。详见 `README.md`。

## 当前开发流程

```bash
npm install       # 首次安装依赖
npm run dev       # 开发（http://localhost:5173）
npm run build     # 类型检查 + 生产构建 → dist/
npm run test      # Vitest 规则测试
```

- 改数值/参数 → `src/config/base/*.json`（六玩法域 + input/tuner）；A/B 对照 → `src/config/variants/`；改技能卡 → `skills.json` 数据；改规则 → `src/core`（补测试）；改输入 → `src/input`；注意力埋点 → `src/telemetry`；皮肤文案 → `src/data/texts.json`。
- `src/core` 为纯规则层，禁止任何 DOM/Canvas；系统函数接收 `state+config+dt+rng`，返回 `GameEvent[]`。
- 不再手工复制打包：部署产物由 `npm run build` 生成于 `dist/`。

## 目录结构

```text
C:\ProjectVL
├─ index.html / package.json / vite.config.ts / tsconfig.json / vitest.config.ts / .gitignore
├─ README.md / 项目总览.md
├─ src/
│  ├─ main.ts                # 薄胶水层
│  ├─ styles/app.css
│  ├─ config/                # base: combat/input/waves/enemies/skills/progression/economy/tuner + variants
│  ├─ data/texts.json        # 皮肤层文案（题材解耦）
│  ├─ core/                  # types / createInitialState / stats / endGame / updateGame + systems/ + effects/(解释器)
│  ├─ render/                # canvasRenderer + draw* 六件
│  ├─ ui/                    # domRefs / renderHud / renderCards / renderEquipment
│  │                         # / tunerPanel / modals / toast / slotFactory / eventText / format
│  ├─ input/                 # pointerRouter / arenaTapResolver / keyboard
│  ├─ telemetry/             # P4.1 注意力遥测（延迟写 localStorage）
│  └─ debug/exposeDebugApi.ts
├─ tests/                    # 系统测试 + effectAtoms / effectInterpreter / configLoader
│                            # / headlessRun / headlessSimulator / balanceV2/v3 + 输入/赏金/遥测
├─ legacy/
│  ├─ single-file/炮台射击_可玩原型_2026-07-07.html   # 重构前基准，可直接双击游玩
│  ├─ backups/               # 任务 04–08 历史 HTML
│  └─ task-records/          # 任务 01–10 过程文档
└─ docs/                     # 炮台射击_游戏立项案.docx、H5 重构提示词等
```

## 版本脉络

1. `01-立项模板` → `10-全量打包`：单文件原型的十期迭代（记录见 `legacy/task-records/`）。
2. **模块化重构（2026-07-07）**：单文件 706 行 IIFE 原型 → 模块化 Vite + TS 工程。分阶段提交：
   归档 → 平移 → 抽数据 → 抽规则(+测试) → 拆表现 → 收尾。玩法行为逐条保持一致。
3. **P3 配置与效果系统重构（2026-07-12）**：config 六域 + variant 机制（A/B 基建）；
   技能=JSON+通用解释器（8 触发器×31 原子，schema v0.3.0）；临时栏移除、消耗释放落点化、
   锁定即装备（方案B暂定为基座默认）、入装门槛 2★/二合/喂养/同类型唯一全配置化。
   详见 docs/下一阶段_决策清单与任务计划.md §P3 交付记录。
4. **P4 数值建模与调参（2026-07-12）**：解析 DPS/EHP/TTK/XP 模型；参数化掉落经济；
   真实 core headless Monte Carlo（标准/宽容/挑战各 3000 局）与经济敏感性（12 场景×4000 局）；
   基座调至 8 普通波+Boss，标准档通关 20.0%、中位 13.75 分钟、过期 12.1%、合成 2.34/普通波。
   交付见 `docs/P4_数值配置_v2.json` 与 `outputs/p4_20260712/ProjectVL_P4_数值设计工作簿_v2.xlsx`。
5. **P4.1 点击注意力补标（2026-07-12）**：统一 Pointer Router、Bounty 主动风险、严格暂停守卫、
   session 遥测与共享动作队列 bot；3难度×3画像×2000局。标准 Target 为18.9%通关、13.62分钟、
   43次有效拾取、2.36合成/普通波、3s/10s峰值2/5、队列P95 1.13秒。交付见
   `docs/P4.1_注意力压力标定说明.md` / JSON v3 / 工作簿 v3。T1真机与T2/T3真人仍未完成。

## Git 状态

- 仓库已初始化，remote 指向 `SageLys/ProjectVL`。
- 本次重构由多个语义化 commit 组成（chore 归档 / build 平移 / refactor 数据·规则·表现 / docs 收尾），
  而非单个巨型提交。
- `node_modules/`、`dist/` 已在 `.gitignore` 中忽略。

## 注意

- `legacy/` 下历史文件为对照与追溯用途，**请勿删除**。
- 平移阶段移除了从未生效的 HTML5 拖拽死代码（`dragPayload`/`parsePayload` 及槽位 drop 监听），
  拖拽仅保留 pointer 实现。
