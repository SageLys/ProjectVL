# Codex 实施 Prompt v2：全卡正式实装（目标定位原语 → 35 卡回填 → 25 配方卡）

> v2 变更说明（回应上一轮 E-1 门禁报告）：你的门禁结论**正确且已被接受**。经根因调查，缺口比你报告的更大——不只是 4 个函数缺失，而是**整层「目标定位原语」不存在**，并且上一轮 35 卡实装已因此被静默摊平（210 支分支中 64 支跨卡字节级同构、24 处设计 `onBreach` 只实装 12 处）。因此本轮范围扩大为「原语层 → 基础卡回填 → 配方卡正式内容」，并新增设计指纹夹具与 V15/V16 防复发。上一版 prompt 的 E-1 清单（把这些能力当作"已存在、只需自检"）**作废**。

## 角色与目标

你是本仓库的实施工程师。目标：让 60 张卡（35 基础卡 + 25 配方产物）**全部达到正式完整实装**，即每张卡每个分支的运行时行为与设计表一致，且不存在跨卡同质化。

**唯一事实来源（冲突时优先级从高到低）**：

1. `docs/目标定位原语与卡牌回填_规格_v1.md` —— 本轮新增：缺失原语 T1–T6 契约（§1）、设计指纹夹具与 V15/V16（§2）、27 组 64 支回填工单（§3）
2. `docs/25配方卡_实装规格_v1.md` —— 配方产物规格：C1–C9 能力（§1.1）、产物字段规则（§2）、B1/B2/B3 逐卡绑定（§3–§5）、测试矩阵（§6）
   **注**：该文 §1.0「复用能力清单」整节作废；其 C6/C7 由 T5/T6 取代统一
3. `docs/五神35卡_完整设计表_v4.md` —— 35 张基础卡的设计源（夹具的填写依据）
4. `docs/卡牌进化系统_重构实行方案_v2.md` —— 进化系统机制背景

数值全部为占位：照规格抄，不自行平衡。

---

## 阶段 A0：审计报告（只读，不改代码）

**不再是通过/停止门禁**，而是产出一份差距报告供人工确认后继续：

1. 复核 §0.1 的引擎事实（interval 原点恒为炮台、aura 只以炮台为心且半径静态、无扇出、无动态缩放）是否与当前 HEAD 一致；不一致处列出
2. 复跑跨卡同构检测：对 35 张基础卡的 210 支分支做绑定深比较（trigger + triggerParams + atoms + params），输出同构组清单，与规格 §3 的 27 组 / 64 支比对，列出差异
3. 复核 D2：按 v4 表逐卡统计 `onBreach`/`onKill`/`onPickup`/`onMerge` 的设计期望数 vs 实装数，列出全部缺失与替换
4. 复核实装中成对追加的 `→statBuff` 绑定是否属于 v4 设计（规格 §3.1 末条）
5. 报告上一轮已确认在位的部分（60 卡、25 条配方、V1–V14、7,500 穷举、即时进化/钉选/导演/结算阶段、黄金回放基线）——这些不必重做

输出后**继续执行 A1**，无需等待人工回复；但若发现规格 §3 工单与实际同构组有实质冲突（例如某组已不存在或多出新组），以实际为准并在提交说明中记录偏差。

## 阶段 A1：目标定位原语层（规格 §1，T1–T6）

- T1 投放原点选择器 `at`（含 `densestCluster` 网格聚类与每帧缓存、平局确定性）｜T2 多目标扇出 `forEach`（maxTargets 必填 ≤8、禁递归、内嵌过 V8）｜T3 跟随位移 `follow`｜T4 半径随时间 `radiusOverTime`（与 `shape:'ring'` 组合）｜T5 动态量缩放 `scaleBy`（source 枚举见规格，`cap` 必填）｜T6 线状投放绑定侧接入 `lineFrom`
- 每条实现**契约 + 融合语义 + 校验规则 + 单元测试**四件套；`interpreter.ts` 顶部契约注释同步
- 固定 seed 可重现性是硬要求：聚类平局、扇出顺序、随机数消耗位置都必须确定
- **禁止按卡内联 if**；T3 若最终只剩 1 个使用方，按规格红线并入 T1

## 阶段 A2：设计指纹夹具与防复发校验（规格 §2）

1. 人工照 `五神35卡_完整设计表_v4.md` 逐条填写 `src/config/base/designFingerprints.json`（35 卡 × 6 分支 ≈ 210 条：`triggers` / `atoms` / `at` / `requires` / `interfaceRole`）
   **严禁**从现有 `skills.json` 反向生成——那会把已摊平的实装固化成"期望"。这一步是本轮质量的总闸门，宁慢勿快
2. 新增 **V15**（跨卡分支绑定指纹唯一，硬失败，无例外）、**V16**（实装 ⊇ 夹具期望：triggers/atoms/at/requires 逐项，缺夹具条目亦失败）、**V17**（identityContract 事件词与触发器一致，警告级）
3. 此时 V15/V16 对现状会大量红灯——**这是预期**，正是 A3 的工单

## 阶段 A3：35 张基础卡回填（规格 §3，27 组 64 支 + §3.1 触发器复位）

1. 按规格 §3 表逐组回填，每组内每支落在不同差异轴（原点 / 扇出集合 / 缩放源 / 形状 / 触发器 / 参数量级）
2. §3.1 D2 专项：`retribution` 3★ 三支 + 5★ 三支全部复位为 `onBreach at:'point'`（当前 3★ 全是 `onHit`，身份契约被抹除）；`sanctum` 5★1 补回 onBreach；其余按夹具逐条复位
3. 清除非设计的配对 `→statBuff` 绑定（跨卡指纹雷同的放大器）
4. 同步更新受影响分支的 `texts.json` 分支文案（禁止跨分支复制同一句）
5. **验收（规格 §3.2）**：V15/V16 全绿、`scripts/audit_content.py` 同构组 = 0、黄金回放的战斗数值漂移**逐项对应本工单条目**（结构性事件流不得漂移）、v4 §7.2 前 90 秒检查表复跑

## 阶段 A4：配方专属能力（`25配方卡_实装规格_v1.md` §1.1 的 C1–C5、C8、C9）

C1 `summon.chainRelay`｜C2 `summon.onDeath` 坐标效果表（`explodeDamageMul` 转译兼容）｜C3 通用计量条 `charge`（HUD 复用进度条）｜C4 `requiresStatus` 数组 AND（与 T2 的 `enemiesWithStatus` 共用判定，不写两份）｜C5 `extraDrop.secure`｜C8 `summonBuff`｜C9 传染 `maxTargets`/`excludeStatus`（若与 T2 的 maxTargets 语义重合，合并为一处实现）

C6/C7 不再单独实现（已由 T5/T6 覆盖）。每条同样四件套 + `fusionOrderInvariance.test.ts` 补 C3/C4 用例。

## 阶段 A5：B1 同神升华 5 张（配方规格 §3）

`stormLattice` / `glacialEpoch` / `volcanoCore` / `aegisCitadel` / `goldenGrove`

灰盒绑定 → 正式绑定（含消耗态三档同值、affixPool 模板、identityContract、name/overview、清除「灰盒占位」字样）；实现 Boss 兼容路径与 `signatureEvent`；逐卡专项测试 + 统一断言集（配方规格 §6.1）。**产物也纳入 V15**（与基础卡及其他产物不得同构）。批次验收后才进 A6。

## 阶段 A6：B2 跨神第一方向 10 张（配方规格 §4）

`thunderRime` / `emberSpark` / `voltBastion` / `ampereFlow` / `rimeShell` / `tombSpire` / `stasisLedger` / `emberMoat` / `emberYield` / `rootLoom`

要求同 A5；每张完成后对照反向卡设计稿（配方方案 §3.x「方向对照」行）复核，实装若使两方向趋同，先改本方向。

## 阶段 A7：B3 跨神反向 10 张（配方规格 §5）

`crystalRelay` / `solarPiercer` / `pylonCircuit` / `midasChain` / `steamBurst` / `glacialEffigy` / `frostDew` / `wrathMortar` / `pyreBrand` / `fortuneThorns`

**开工前**：同神对的 B2 产物逐对固定 seed 并排试跑，记录差异确认不趋同，写入提交说明。注意规格已明示的降级：`pylonCircuit` 塔间网线判定带 → 塔自带 aura。

## 阶段 A8：数值冒烟 + 全量回归（配方规格 §6.3）

1. 固定 seed 三场景（波 7 密集 / 波 9 验证 / 波 10 Boss）：装备产物 vs 双 5★ 材料 vs 单 6★ 材料，产物核心贡献 ∈ [1.1, 1.8]× 单 6★ 材料当量；越界只调占位参数并**回写规格文档**
2. 全 25 张 `signatureEvent` 断言：进化后 5s 内首个专属表现出现
3. 全量回归：`recipeOnly` 池隔离；进化事务对账链对新产物（墙体/金树/根脉/晶柱）生效；2 次上限与 `stasisLedger`「进化触发 onMerge」无递归；V1–V17 全绿；`npm test` / `npm run build` / `npm run validate`
4. 更新内容设计工作台矩阵视图：产物状态灰盒 → 正式；输出最终一致性报告（60 卡 × 分支数 / 同构组 0 / 夹具符合率 100%）

---

## 红线（违反即返工）

1. **禁止为单张卡或单张产物在核心层写 if**；通用能力必须服务 ≥2 张卡，否则回规格合并或降参数
2. **禁止用"实装近似"替代缺失能力**——这正是本轮要修的根因。缺能力就在 A1/A4 补能力；确实无法实现时**停下报告**，不得静默降级
3. **禁止从 `skills.json` 反向生成设计指纹夹具**
4. 运行时红线沿用：onKill/onBreach 按事件坐标；`requiresSource` ∈ {weapon,chain,dot}；绑定内不重复取最大/覆盖原子；「每命中各触发一次」只走贯穿/分裂；`forEach`/`onDeath`/`trailZone`/`charge` 释放的效果表同样过 V8
5. 产物身份原子只许来自 `sourceGods` 两神（V6 白名单）；`chainRelay`/`extraDrop.secure`/`trailZone` 仅 `recipeOnly` 卡可用
6. 不实现分支回响/词条继承；不动遗物系统与 `buildModifierSystem`；不改 25 格矩阵与配方数据结构
7. 文案只写功能性占位，不写题材文案；「灰盒占位」字样清零
8. 固定 seed 可重现性不得破坏（聚类平局、扇出顺序、随机数消耗位置确定）

## 提交顺序

A0 审计报告 → A1 原语（独立提交）→ A2 夹具+V15/V16（独立提交，预期大量红灯）→ A3 回填（分神提交亦可，收尾时 V15/V16 必须全绿）→ A4 配方能力 → A5 → A6 → A7（每批独立提交，附验收清单）→ A8 冒烟与最终报告。

每个阶段提交说明须包含：本阶段验收清单勾选结果、与规格的任何偏差及理由、黄金回放漂移逐项归因。
